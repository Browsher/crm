# Fatia `cep` — plano de implementação

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Carregar a base de CEP do OpenCEP no próprio Postgres e expor duas
funções para a fatia `empresas` resolver endereço por `SELECT` local.

**Architecture:** Uma migração cria `cep` (1,2 milhão de linhas de referência,
leitura irrestrita para quem está autenticado) e `cep_carga` (o retrato: versão,
data e sha256 do arquivo). O dado entra por script operacional com
`DATABASE_URL_ADMIN`, nunca pela aplicação — `app_usuario` só tem `SELECT`. Uma
invariante nova cataloga a primeira política de leitura irrestrita do projeto.

**Tech Stack:** TypeScript sem ponto e vírgula, aspas simples. Vitest (projetos
`unitario` e `integracao`). Postgres 17 no container. `yauzl` para ler o zip em
fluxo e `pg-copy-streams` para o `COPY ... FROM STDIN`, os dois como
`devDependencies`.

**Spec:** `docs/superpowers/specs/2026-09-09-cep-design.md`

## Global Constraints

- **Branch `fatia-cep`, nunca commit na `main`.** Conferir
  `git branch --show-current` **imediatamente antes** de cada commit (R-013).
- **Migração aplicada é imutável, byte a byte.** Corrigir é acrescentar migração
  nova. O `.sql` leva no máximo uma linha de comentário no topo, começando com
  `--`, apontando para `docs/db/NNNN.md`. `BEGIN;` na primeira linha de código,
  `COMMIT;` na última, nenhum controle de transação no meio.
- **Toda tabela nova entra com RLS.**
- **TDD: teste falhando primeiro, sempre.** Escrever implementação antes de ver
  o vermelho é motivo para desfazer e refazer.
- **Regra de negócio devolve `{ ok, motivo }`, não `throw`.** Exceção é falha de
  infraestrutura. `resolverCeps` é a exceção deliberada, justificada na spec:
  consulta de referência não decide negócio.
- **`features/` não importa de outra `features/`.** Por isso o módulo vive em
  `src/server/cep/`.
- **Sem `--force` nem `--legacy-peer-deps` no npm** (R-003). Peer dependency
  incompatível se resolve subindo a versão.
- **Nomes de arquivo, função e coluna em português**, como todo o repositório.
- Caminho do zip nesta máquina:
  `C:\Users\ADM\projetos\.bases\opencep-2.0.1.zip`, sha256
  `cffa33783b7cb2bec060acd499eb8356698918efdc79238708aac46fcce9e62d`. É
  referência da máquina, não convenção: o script recebe o caminho por argumento.

---

## File Structure

| Arquivo | Responsabilidade |
|---|---|
| `db/migracoes/0013_cep.sql` | cria `cep` e `cep_carga`, RLS, política e `GRANT` |
| `docs/db/0013.md` | o porquê da migração |
| `src/server/db/migracoes/invariantes.ts` | ganha `POLITICAS_DE_LEITURA_IRRESTRITA` e a checagem |
| `src/server/cep/linha.ts` | puro: JSON de um CEP → linha; linha → campo CSV |
| `src/server/cep/zip.ts` | percorre o zip chamando um callback por CEP |
| `src/server/cep/manifesto.ts` | lê e valida o manifesto; confere o sha256 do zip |
| `src/server/cep/carregar.ts` | a transação: temp, `COPY`, `DISTINCT ON`, contagem, `cep_carga` |
| `src/server/cep/resolver.ts` | `normalizarCep` e `resolverCeps` — o contrato de `empresas` |
| `scripts/db/cep-carregar.mts` | CLI fino: argumentos, `urlDe`, `sair` |
| `db/cep/opencep.json` | manifesto versionado da base real |
| `tests/fixtures/cep-mini.zip` | zip de 12 entradas, 11 CEPs distintos |
| `tests/integracao/cep.test.ts` | políticas, privilégios e `resolverCeps` |
| `tests/integracao/cep-carregar.test.ts` | o carregador ponta a ponta contra o fixture |

---

### Task 1: A invariante de leitura irrestrita

Ela entra **antes** da migração, com a lista **vazia**. Assim ela é verde contra
o banco de hoje, e a Task 2 acrescenta a tabela e o nome na lista no mesmo
commit — que é a disciplina que a spec pede: a catraca nasce com o caso que
vigia.

**Files:**
- Modify: `src/server/db/migracoes/invariantes.ts`
- Test: `src/server/db/migracoes/invariantes.test.ts`

**Interfaces:**
- Consumes: `Estado` e `avaliar` já existentes.
- Produces: `POLITICAS_DE_LEITURA_IRRESTRITA: readonly string[]`, e o campo
  `politicasIrrestritas: string[]` em `Estado`. Nomes no formato
  `schema.tabela.politica`.

- [ ] **Step 1: Escrever o teste que falha**

Em `src/server/db/migracoes/invariantes.test.ts`, acrescentar
`politicasIrrestritas: [],` ao objeto devolvido por `sao()` (junto das outras
listas vazias, depois de `politicasEmAutenticacao: [],`), e acrescentar no fim
do arquivo:

```ts
describe('políticas de leitura irrestrita', () => {
  test('política com USING (true) fora da lista é acusada', () => {
    const e = sao()
    e.politicasIrrestritas = ['public.pedido.pedido_ler']
    umaViolacao(e, /leitura irrestrita não registrada: public\.pedido\.pedido_ler/)
  })

  test('nenhuma política irrestrita no banco não é violação', () => {
    expect(avaliar(sao())).toEqual([])
  })
})
```

- [ ] **Step 2: Rodar e ver falhar**

Run: `npm run test:unit -- invariantes`
Expected: FAIL — o TypeScript recusa `politicasIrrestritas` (não existe em
`Estado`), e o teste da violação falha porque `avaliar` não a produz.

- [ ] **Step 3: Implementar**

Em `src/server/db/migracoes/invariantes.ts`, logo depois da constante
`FUNCOES_CONCEDIDAS_A_APP_USUARIO`:

```ts
// Pergunta 3: quais políticas podem liberar a tabela inteira. `USING (true)`
// não anula a RLS — declara que o dado é público para quem está autenticado.
// A regra 8 só garante que a RLS foi LIGADA: "RLS sem política" (nega tudo) e
// "RLS com USING (true)" (libera tudo) são idênticas para os dois booleanos de
// pg_class, e são opostas em efeito. Esta lista é a diferença.
//
// LIMITE HONESTO: pega só `true` literal em USING. `USING (1=1)` escapa, e
// WITH CHECK irrestrito (escrita) não é olhado. É catraca contra descuido e
// cópia, não contra quem quer burlar — mesma classe de limite da busca por
// prosrc. Está escrito para a lista não parecer mais forte do que é.
export const POLITICAS_DE_LEITURA_IRRESTRITA = [] as readonly string[]
```

No tipo `Estado`, acrescentar depois de `politicasEmAutenticacao: string[]`:

```ts
  politicasIrrestritas: string[]
```

Em `lerEstado`, depois da consulta `politicas`:

```ts
  // pg_policies.qual é o texto do USING já normalizado pelo Postgres:
  // `USING (true)` volta como 'true'. Nome no formato schema.tabela.politica.
  const irrestritas = await c.query<{ nome: string }>(`
    SELECT schemaname || '.' || tablename || '.' || policyname AS nome
    FROM pg_policies
    WHERE schemaname NOT IN ('pg_catalog', 'information_schema')
      AND qual = 'true'
    ORDER BY 1`)
```

No objeto devolvido por `lerEstado`, depois de `politicasEmAutenticacao`:

```ts
    politicasIrrestritas: irrestritas.rows.map((r) => r.nome),
```

Em `avaliar`, logo antes do laço de `funcoesExecutaveisPorPublico`:

```ts
  const irrestritasRegistradas = new Set<string>(POLITICAS_DE_LEITURA_IRRESTRITA)
  for (const nome of e.politicasIrrestritas) {
    if (!irrestritasRegistradas.has(nome)) {
      v.push(`política de leitura irrestrita não registrada: ${nome} (USING (true) libera a tabela para todo app_usuario)`)
    }
  }
  const irrestritasNoBanco = new Set(e.politicasIrrestritas)
  for (const nome of POLITICAS_DE_LEITURA_IRRESTRITA) {
    if (!irrestritasNoBanco.has(nome)) v.push(`política de leitura irrestrita registrada e ausente: ${nome}`)
  }
```

- [ ] **Step 4: Rodar e ver passar**

Run: `npm run test:unit -- invariantes`
Expected: PASS.

- [ ] **Step 5: Provar que a consulta nova roda contra banco de verdade**

Run: `npm run db:subir && npm run test:integracao -- schema`
Expected: PASS. Isto exercita `lerEstado` inteiro; se a consulta de
`pg_policies` tiver erro de SQL, quebra aqui e não na Task 2.

- [ ] **Step 6: Commit**

```bash
git branch --show-current   # tem que dizer fatia-cep
git add src/server/db/migracoes/invariantes.ts src/server/db/migracoes/invariantes.test.ts
git commit -m "catraca: politicas de leitura irrestrita catalogadas

As invariantes de RLS leem so relrowsecurity e relforcerowsecurity, entao
RLS sem politica (nega tudo) e RLS com USING (true) (libera tudo) sao
identicas para a catraca e opostas em efeito. Lista fechada dos dois lados,
irma de FUNCOES_CONCEDIDAS_A_APP_USUARIO. Nasce vazia; a primeira entrada
vem junto com a tabela que a justifica."
```

---

### Task 2: Migração `0013_cep.sql`, políticas e privilégios

**Files:**
- Create: `db/migracoes/0013_cep.sql`
- Create: `docs/db/0013.md`
- Create: `tests/integracao/cep.test.ts`
- Modify: `src/server/db/migracoes/invariantes.ts` (uma entrada na lista)
- Modify: `src/server/db/migracoes/invariantes.test.ts` (o `sao()` e um teste)

**Interfaces:**
- Consumes: `POLITICAS_DE_LEITURA_IRRESTRITA` da Task 1.
- Produces: tabelas `cep` (colunas `cep`, `logradouro`, `faixa`, `bairro`,
  `localidade`, `uf`, `ibge`) e `cep_carga` (colunas `id`, `fonte`, `versao`,
  `publicado_em`, `arquivo_sha256`, `linhas`, `carregado_em`). `SELECT` em `cep`
  para `app_usuario`; nada em `cep_carga` para ninguém.

- [ ] **Step 1: Escrever o teste que falha**

Criar `tests/integracao/cep.test.ts`:

```ts
import { afterAll, beforeAll, describe, expect, test } from 'vitest'
import { conectarVerificado } from '@/src/server/db/pool'
import { criarBancoDeTeste, criarUsuario, type BancoDeTeste } from './ajuda'

let banco: BancoDeTeste
let vendedor: string

beforeAll(async () => {
  banco = await criarBancoDeTeste()
  vendedor = await criarUsuario(banco, 'vendedor', 'Vendedor')
  await banco.sql(
    `INSERT INTO cep (cep, logradouro, faixa, bairro, localidade, uf, ibge)
     VALUES ('01310100', 'Avenida Paulista', 'lado ímpar', 'Bela Vista', 'São Paulo', 'SP', '3550308')`,
  )
})
afterAll(async () => {
  await banco.derrubar()
})

describe('cep: leitura', () => {
  test('qualquer autenticado lê, inclusive vendedor', async () => {
    const linhas = await banco.comoUsuario(vendedor, async (e) => {
      const r = await e<{ localidade: string }>('SELECT localidade FROM cep WHERE cep = $1', ['01310100'])
      return r.linhas
    })
    expect(linhas).toEqual([{ localidade: 'São Paulo' }])
  })

  test('app_conexao fora de comoUsuario é permission denied, não zero linhas', async () => {
    const c = await conectarVerificado(banco.urlApp)
    try {
      await expect(c.query('SELECT cep FROM cep')).rejects.toMatchObject({ code: '42501' })
    } finally {
      c.release()
    }
  })
})

describe('cep: ninguém escreve pela aplicação', () => {
  const escritas: [string, string][] = [
    ['INSERT', "INSERT INTO cep (cep, localidade, uf, ibge) VALUES ('00000000', 'X', 'SP', '3550308')"],
    ['UPDATE', "UPDATE cep SET localidade = 'X' WHERE cep = '01310100'"],
    ['DELETE', "DELETE FROM cep WHERE cep = '01310100'"],
  ]
  for (const [verbo, sql] of escritas) {
    test(`${verbo} é 42501 por falta de GRANT`, async () => {
      await expect(banco.comoUsuario(vendedor, (e) => e(sql))).rejects.toMatchObject({ code: '42501' })
    })
  }
})

describe('cep_carga: inalcançável pela aplicação', () => {
  test('SELECT é 42501', async () => {
    await expect(
      banco.comoUsuario(vendedor, (e) => e('SELECT versao FROM cep_carga')),
    ).rejects.toMatchObject({ code: '42501' })
  })

  test('a dona lê: a tabela existe e o teste acima não passou por ausência', async () => {
    const r = await banco.sql<{ n: string }>('SELECT count(*)::text AS n FROM cep_carga')
    expect(r).toEqual([{ n: '0' }])
  })
})
```

Acrescentar em `src/server/db/migracoes/invariantes.test.ts`, dentro do
`describe('políticas de leitura irrestrita')`, e trocar
`politicasIrrestritas: []` por `politicasIrrestritas: ['public.cep.cep_leitura']`
no `sao()`:

```ts
  test('política registrada que sumiu do banco é acusada', () => {
    const e = sao()
    e.politicasIrrestritas = []
    umaViolacao(e, /leitura irrestrita registrada e ausente: public\.cep\.cep_leitura/)
  })
```

- [ ] **Step 2: Rodar e ver falhar**

Run: `npm run test:integracao -- cep`
Expected: FAIL com `relation "cep" does not exist` já no `beforeAll`.

- [ ] **Step 3: Escrever a migração**

Criar `db/migracoes/0013_cep.sql`:

```sql
-- ver docs/db/0013.md
BEGIN;
CREATE TABLE cep (
  cep         text PRIMARY KEY CHECK (cep ~ '^[0-9]{8}$'),
  logradouro  text,
  faixa       text,
  bairro      text,
  localidade  text NOT NULL CHECK (btrim(localidade) <> ''),
  uf          char(2) NOT NULL CHECK (uf ~ '^[A-Z]{2}$'),
  ibge        text NOT NULL CHECK (ibge ~ '^[0-9]{7}$')
);
CREATE TABLE cep_carga (
  id             uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  fonte          text NOT NULL,
  versao         text NOT NULL,
  publicado_em   date NOT NULL,
  arquivo_sha256 char(64) NOT NULL CHECK (arquivo_sha256 ~ '^[0-9a-f]{64}$'),
  linhas         integer NOT NULL CHECK (linhas > 0),
  carregado_em   timestamptz NOT NULL DEFAULT now()
);
ALTER TABLE cep ENABLE ROW LEVEL SECURITY;
ALTER TABLE cep_carga ENABLE ROW LEVEL SECURITY;
CREATE POLICY cep_leitura ON cep FOR SELECT TO app_usuario USING (true);
GRANT SELECT ON cep TO app_usuario;
COMMIT;
```

Registrar a política na lista, em `src/server/db/migracoes/invariantes.ts`:

```ts
export const POLITICAS_DE_LEITURA_IRRESTRITA = ['public.cep.cep_leitura'] as readonly string[]
```

- [ ] **Step 4: Escrever `docs/db/0013.md`**

```markdown
# 0013 — cep e cep_carga

Base de CEP local, para a fatia `empresas` resolver endereço por `SELECT` em vez
de API. Desenho completo em
`docs/superpowers/specs/2026-09-09-cep-design.md`.

## Duas tabelas

`cep` é referência: 1,2 milhão de linhas, o Brasil inteiro, igual para todo
mundo. `cep_carga` é o retrato — qual versão da base está carregada, de que data
e com que sha256.

`cep_carga` responde a pergunta que sem ela não tem resposta: um CEP que não
resolve **não existe, ou é mais novo que a base?** A base do OpenCEP está parada
em julho de 2024, então a segunda hipótese é a provável, e sem a data gravada as
duas são indistinguíveis.

## `faixa`, e não `complemento`

No arquivo dos Correios esse campo é o trecho da rua que o CEP cobre — `lado
ímpar`, `lado par`, `s/n`. Mas `empresa` vai ter uma coluna `complemento` que
significa "sala 302". Duas colunas com o mesmo nome e significados diferentes,
lado a lado num `JOIN`, é erro silencioso: os dois campos são texto e os dois
pareceriam plausíveis numa tela.

## Por que só três colunas são NOT NULL

Medido no arquivo real, 1.209.313 linhas: `localidade`, `uf` e `ibge` não têm um
único furo. `logradouro` falta em 10.392 (0,86%) e `bairro` em 7.200 (0,6%) — são
os CEPs únicos de cidade pequena, onde o município inteiro tem um CEP só. O furo
tem significado, então a coluna é anulável.

## `USING (true)`: a primeira leitura irrestrita do projeto

Não anula a RLS. A regra 8 exige RLS em toda tabela nova, e RLS ligada **sem**
política nega tudo; `USING (true)` é a declaração explícita de que o dado é
público para quem está autenticado. E "autenticado" não é frouxidão:
`app_usuario` só é alcançável por dentro de `comoUsuario`, que exige um
`usuario_id`. Fora dali, `app_conexao` não herda nada e a tabela dá `42501`.

`cep` não tem dado de pessoa — é a lista de logradouros do Brasil. Não há o que
mascarar. **A próxima tabela não copia este padrão sem o mesmo argumento**, e a
invariante `POLITICAS_DE_LEITURA_IRRESTRITA` existe para cobrar isso.

## `cep_carga` sem GRANT nenhum

R-014 de propósito: hoje nenhuma tela lê a data da base. O consumidor natural é o
relatório de importação de `empresas`, e ele é da fatia seguinte — o `GRANT`
entra junto com o relatório, na mesma mudança, ou não entra. RLS ligada sem
política nega tudo para todos, que é o acesso certo por enquanto.

## Escrita

Nenhuma, pela aplicação. O dado entra por `npm run db:cep:carregar`, com
`DATABASE_URL_ADMIN`, como o `db:aplicar`. Dar escrita a `app_usuario` seria
conceder superfície que nenhuma tela usa (R-014).
```

- [ ] **Step 5: Aplicar e rodar tudo**

Run: `npm run db:checar && npm run db:aplicar && npm run test`
Expected: PASS. O `db:aplicar` termina com "invariantes ok" — se a política não
estivesse registrada, ele pararia com "leitura irrestrita não registrada", que é
a catraca funcionando.

- [ ] **Step 6: Commit**

```bash
git branch --show-current
git add db/migracoes/0013_cep.sql docs/db/0013.md tests/integracao/cep.test.ts \
        src/server/db/migracoes/invariantes.ts src/server/db/migracoes/invariantes.test.ts
git commit -m "0013: tabelas cep e cep_carga

cep e referencia com leitura irrestrita para quem esta autenticado, primeira
do projeto; a politica entra na lista da catraca no mesmo commit. cep_carga
guarda o retrato (versao, data, sha256) e nao tem GRANT nenhum ate existir
tela que leia a data da base. Escrita so pelo script operacional."
```

---

### Task 3: `normalizarCep`

**Files:**
- Create: `src/server/cep/resolver.ts`
- Test: `src/server/cep/resolver.test.ts`

**Interfaces:**
- Produces: `normalizarCep(bruto: string): string | null`. Consumida pela fase 1
  da importação de `empresas` e pela Task 4.

- [ ] **Step 1: Escrever o teste que falha**

Criar `src/server/cep/resolver.test.ts`:

```ts
import { describe, expect, test } from 'vitest'
import { normalizarCep } from './resolver'

describe('normalizarCep', () => {
  test.each([
    ['01310-100', '01310100'],
    ['01310100', '01310100'],
    ['  01310100  ', '01310100'],
    ['01310.100', '01310100'],
    ['01310 100', '01310100'],
  ])('%s vira %s', (bruto, esperado) => {
    expect(normalizarCep(bruto)).toBe(esperado)
  })

  test.each([
    ['', 'vazio'],
    ['   ', 'só espaço'],
    ['0131010', 'sete dígitos'],
    ['013101000', 'nove dígitos'],
    ['consultar', 'texto'],
    ['0131010a', 'letra no meio'],
  ])('%s (%s) vira null', (bruto) => {
    expect(normalizarCep(bruto)).toBeNull()
  })

  test('não confunde separador com dígito: 12-34-56-78 tem oito dígitos', () => {
    expect(normalizarCep('12-34-56-78')).toBe('12345678')
  })
})
```

- [ ] **Step 2: Rodar e ver falhar**

Run: `npm run test:unit -- resolver`
Expected: FAIL — `Failed to resolve import './resolver'`.

- [ ] **Step 3: Implementar**

Criar `src/server/cep/resolver.ts`:

```ts
// Pura: sem banco, sem rede. É a fase 1 da importação de empresas, que precisa
// recusar 'CEP: consultar' numa célula sem abrir conexão.
// Só remove separadores; qualquer letra sobrevive à limpeza e reprova no teste
// de oito dígitos, então 'a1310100' não vira '1310100'.
export function normalizarCep(bruto: string): string | null {
  const limpo = bruto.replace(/[\s.\-/]/g, '')
  return /^[0-9]{8}$/.test(limpo) ? limpo : null
}
```

- [ ] **Step 4: Rodar e ver passar**

Run: `npm run test:unit -- resolver`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git branch --show-current
git add src/server/cep/resolver.ts src/server/cep/resolver.test.ts
git commit -m "cep: normalizarCep, a validacao sem banco

Fase 1 da importacao de empresas: recusa celula invalida antes de qualquer
conexao. Remove so separadores, entao letra sobrevive a limpeza e reprova
nos oito digitos em vez de virar CEP curto valido."
```

---

### Task 4: `resolverCeps`

**Files:**
- Modify: `src/server/cep/resolver.ts`
- Modify: `tests/integracao/cep.test.ts`

**Interfaces:**
- Consumes: `Executar` de `@/src/server/db/como-usuario`; a tabela `cep` da
  Task 2.
- Produces: `type Endereco` e
  `resolverCeps(executar: Executar, ceps: string[]): Promise<Map<string, Endereco>>`.
  É o contrato que `empresas` consome.

- [ ] **Step 1: Escrever o teste que falha**

Em `tests/integracao/cep.test.ts`, acrescentar os imports
`import { resolverCeps } from '@/src/server/cep/resolver'` e
`import type { Executar } from '@/src/server/db/como-usuario'` e, no `beforeAll`,
uma segunda linha depois da de Avenida Paulista:

```ts
  await banco.sql(
    `INSERT INTO cep (cep, logradouro, faixa, bairro, localidade, uf, ibge)
     VALUES ('69900001', NULL, NULL, NULL, 'Rio Branco', 'AC', '1200401')`,
  )
```

E no fim do arquivo:

```ts
describe('resolverCeps', () => {
  test('traz o que existe e omite o que não existe, sem lançar', async () => {
    const mapa = await banco.comoUsuario(vendedor, (e) =>
      resolverCeps(e, ['01310100', '00000000', '69900001']),
    )
    expect(mapa.size).toBe(2)
    expect(mapa.get('01310100')).toEqual({
      cep: '01310100',
      logradouro: 'Avenida Paulista',
      faixa: 'lado ímpar',
      bairro: 'Bela Vista',
      localidade: 'São Paulo',
      uf: 'SP',
      ibge: '3550308',
    })
    expect(mapa.get('00000000')).toBeUndefined()
  })

  test('CEP sem logradouro nem bairro volta com null, não com string vazia', async () => {
    const mapa = await banco.comoUsuario(vendedor, (e) => resolverCeps(e, ['69900001']))
    expect(mapa.get('69900001')).toMatchObject({ logradouro: null, faixa: null, bairro: null })
  })

  test('uma consulta só, não uma por CEP', async () => {
    let idas = 0
    // Um cast só, no ponto onde a função concreta encontra a assinatura
    // genérica de Executar. Espalhar `as never` pelos argumentos esconderia
    // erro de tipo de verdade.
    const espiao = (async (sql: string, params?: unknown[]) => {
      idas++
      return banco.comoUsuario(vendedor, (e) => e(sql, params))
    }) as Executar
    await resolverCeps(espiao, ['01310100', '69900001', '00000000'])
    expect(idas).toBe(1)
  })

  test('lista vazia nem chega a consultar', async () => {
    let idas = 0
    const espiao = (async (sql: string, params?: unknown[]) => {
      idas++
      return banco.comoUsuario(vendedor, (e) => e(sql, params))
    }) as Executar
    const mapa = await resolverCeps(espiao, [])
    expect(mapa.size).toBe(0)
    expect(idas).toBe(0)
  })
})
```

- [ ] **Step 2: Rodar e ver falhar**

Run: `npm run test:integracao -- cep`
Expected: FAIL — `resolverCeps` não é exportada por `resolver.ts`.

- [ ] **Step 3: Implementar**

Acrescentar em `src/server/cep/resolver.ts`:

```ts
import type { Executar } from '../db/como-usuario'

export type Endereco = {
  cep: string
  logradouro: string | null
  faixa: string | null
  bairro: string | null
  localidade: string
  uf: string
  ibge: string
}

// Em lote de propósito. A importação de empresas tem milhares de linhas e
// algumas centenas de CEPs distintos: `= ANY` resolve todas numa ida. A versão
// singular convidaria a um laço com centenas de idas, que mede bem em teste com
// dez linhas e só aparece com a planilha real.
//
// Devolve mapa PARCIAL, e não { ok, motivo }, contrariando o padrão do projeto
// de propósito: CEP não encontrado não é falha da operação. A consulta
// funcionou e a resposta é "não existe na base de julho/2024". Quem decide o
// que fazer com isso é a importação, que sabe que a empresa entra mesmo assim.
// Consulta de referência não decide negócio.
export async function resolverCeps(executar: Executar, ceps: string[]): Promise<Map<string, Endereco>> {
  if (ceps.length === 0) return new Map()
  const { linhas } = await executar<Endereco>(
    `SELECT cep, logradouro, faixa, bairro, localidade, uf, ibge
       FROM cep WHERE cep = ANY($1)`,
    [ceps],
  )
  return new Map(linhas.map((l) => [l.cep, l]))
}
```

- [ ] **Step 4: Rodar e ver passar**

Run: `npm run test:integracao -- cep`
Expected: PASS. O teste do `uf` merece atenção: a coluna é `char(2)`, e o
Postgres devolve `'SP'` sem preenchimento porque já tem dois caracteres. Se
algum dia virar `char(3)`, este teste pega.

- [ ] **Step 5: Commit**

```bash
git branch --show-current
git add src/server/cep/resolver.ts tests/integracao/cep.test.ts
git commit -m "cep: resolverCeps, o contrato que empresas consome

Lote com = ANY, uma ida ao banco por importacao inteira. Mapa parcial em vez
de { ok, motivo }: CEP nao encontrado nao e falha da operacao, e consulta de
referencia nao decide negocio — quem decide e a importacao."
```

---

### Task 5: Ler o zip — a parte pura e o fixture

**Files:**
- Create: `src/server/cep/linha.ts`
- Create: `src/server/cep/linha.test.ts`
- Create: `src/server/cep/zip.ts`
- Create: `tests/fixtures/cep-mini.zip`
- Create: `tests/integracao/cep-carregar.test.ts`
- Modify: `.gitattributes`
- Modify: `package.json` (devDependencies)

**Interfaces:**
- Produces: `type LinhaCep`, `linhaDoJson(texto: string): LinhaCep | null`,
  `campoCsv(v: string | null): string`, `paraCsv(l: LinhaCep): string`, e
  `lerZip(caminho: string, aoLer: (l: LinhaCep) => void): Promise<{ lidas: number; descartadas: number }>`.

- [ ] **Step 1: Blindar o `.gitattributes` antes de o fixture existir**

Acrescentar em `.gitattributes`:

```
*.zip -text
```

Isto vem **antes** de criar o zip, não depois. Hoje o arquivo é
`* text=auto eol=lf`, e o `text=auto` provavelmente detectaria o binário e
deixaria quieto — mas "provavelmente o git faz a coisa certa" é exatamente o que
custou a tarde de diagnóstico errado da **R-011**, onde a suspeita caiu no banco
e o culpado era o checkout em CRLF.

- [ ] **Step 2: Instalar as dependências**

Run: `npm i -D yauzl @types/yauzl pg-copy-streams @types/pg-copy-streams`

As quatro são `devDependencies`: o carregador é operação, não runtime, e nada
disso pode entrar no build da aplicação. Se algum peer dependency reclamar, subir
a versão do pacote antigo — **nunca `--force` nem `--legacy-peer-deps`** (R-003).

- [ ] **Step 3: Escrever o teste que falha (parte pura)**

Criar `src/server/cep/linha.test.ts`:

```ts
import { describe, expect, test } from 'vitest'
import { campoCsv, linhaDoJson, paraCsv } from './linha'

const completo = JSON.stringify({
  cep: '01001-000',
  logradouro: 'Praça da Sé',
  complemento: 'lado ímpar',
  bairro: 'Sé',
  localidade: 'São Paulo',
  uf: 'SP',
  ibge: '3550308',
})

describe('linhaDoJson', () => {
  test('tira o hífen do cep e move complemento para faixa', () => {
    expect(linhaDoJson(completo)).toEqual({
      cep: '01001000',
      logradouro: 'Praça da Sé',
      faixa: 'lado ímpar',
      bairro: 'Sé',
      localidade: 'São Paulo',
      uf: 'SP',
      ibge: '3550308',
    })
  })

  test('string vazia vira null, não string vazia', () => {
    const texto = JSON.stringify({
      cep: '69900-001', logradouro: '', complemento: '', bairro: '',
      localidade: 'Rio Branco', uf: 'AC', ibge: '1200401',
    })
    expect(linhaDoJson(texto)).toMatchObject({ logradouro: null, faixa: null, bairro: null })
  })

  test.each([
    ['{ isto não é json', 'json quebrado'],
    ['{"cep":"123"}', 'cep curto'],
    ['{"cep":"01001-000","localidade":"","uf":"SP","ibge":"3550308"}', 'localidade vazia'],
    ['{"cep":"01001-000","localidade":"X","uf":"","ibge":"3550308"}', 'uf vazia'],
    ['{"cep":"01001-000","localidade":"X","uf":"SP","ibge":"355030"}', 'ibge com seis dígitos'],
  ])('%s (%s) vira null', (texto) => {
    expect(linhaDoJson(texto)).toBeNull()
  })
})

describe('campoCsv', () => {
  test.each([
    [null, ''],
    ['Rua Simples', 'Rua Simples'],
    ['Rua A, 100', '"Rua A, 100"'],
    ['Rua "Aspas"', '"Rua ""Aspas"""'],
    ['Rua\ncom quebra', '"Rua\ncom quebra"'],
  ])('%s vira %s', (entrada, esperado) => {
    expect(campoCsv(entrada)).toBe(esperado)
  })

  test('null vira campo vazio SEM aspas, que é como o COPY CSV lê NULL', () => {
    // No COPY ... WITH (FORMAT csv), campo vazio sem aspas é NULL e "" é string
    // vazia. Como nunca guardamos string vazia, a distinção é inequívoca.
    expect(campoCsv(null)).toBe('')
    expect(campoCsv('')).toBe('""')
  })
})

describe('paraCsv', () => {
  test('sete campos na ordem das colunas, terminando em quebra de linha', () => {
    expect(paraCsv(linhaDoJson(completo)!)).toBe(
      '01001000,Praça da Sé,lado ímpar,Sé,São Paulo,SP,3550308\n',
    )
  })
})
```

- [ ] **Step 4: Rodar e ver falhar**

Run: `npm run test:unit -- linha`
Expected: FAIL — `Failed to resolve import './linha'`.

- [ ] **Step 5: Implementar a parte pura**

Criar `src/server/cep/linha.ts`:

```ts
export type LinhaCep = {
  cep: string
  logradouro: string | null
  faixa: string | null
  bairro: string | null
  localidade: string
  uf: string
  ibge: string
}

const vazioViraNulo = (v: unknown): string | null => {
  if (typeof v !== 'string') return null
  const t = v.trim()
  return t === '' ? null : t
}

// O campo `complemento` do arquivo dos Correios NÃO é complemento de endereço:
// é o trecho da rua que o CEP cobre ('lado ímpar', 's/n'). Vira `faixa` aqui,
// para não colidir com empresa.complemento, que significa 'sala 302'.
export function linhaDoJson(texto: string): LinhaCep | null {
  let d: Record<string, unknown>
  try {
    d = JSON.parse(texto) as Record<string, unknown>
  } catch {
    return null
  }
  const cep = (vazioViraNulo(d.cep) ?? '').replace(/-/g, '')
  const localidade = vazioViraNulo(d.localidade)
  const uf = vazioViraNulo(d.uf)?.toUpperCase() ?? null
  const ibge = vazioViraNulo(d.ibge)
  if (!/^[0-9]{8}$/.test(cep)) return null
  if (localidade === null) return null
  if (uf === null || !/^[A-Z]{2}$/.test(uf)) return null
  if (ibge === null || !/^[0-9]{7}$/.test(ibge)) return null
  return {
    cep,
    logradouro: vazioViraNulo(d.logradouro),
    faixa: vazioViraNulo(d.complemento),
    bairro: vazioViraNulo(d.bairro),
    localidade,
    uf,
    ibge,
  }
}

// No COPY ... WITH (FORMAT csv) o campo vazio SEM aspas é NULL e `""` é string
// vazia. Como linhaDoJson já transformou vazio em null, a distinção é
// inequívoca: null vira nada, e nunca escrevemos string vazia.
export function campoCsv(v: string | null): string {
  if (v === null) return ''
  return /["\n\r,]/.test(v) ? `"${v.replace(/"/g, '""')}"` : v
}

export function paraCsv(l: LinhaCep): string {
  const campos = [l.cep, l.logradouro, l.faixa, l.bairro, l.localidade, l.uf, l.ibge]
  return `${campos.map(campoCsv).join(',')}\n`
}
```

Nota: `campoCsv('')` devolve `'""'` porque a regex não casa e a string vazia sai
crua — **isso está errado**. Corrigir escrevendo a guarda explícita:

```ts
export function campoCsv(v: string | null): string {
  if (v === null) return ''
  if (v === '') return '""'
  return /["\n\r,]/.test(v) ? `"${v.replace(/"/g, '""')}"` : v
}
```

- [ ] **Step 6: Rodar e ver passar**

Run: `npm run test:unit -- linha`
Expected: PASS.

- [ ] **Step 7: Construir o fixture**

Rodar este comando uma vez, na raiz do projeto. Ele cria doze entradas com onze
CEPs distintos — o repetido existe para provar a deduplicação, porque a base real
tem exatamente uma duplicata e sem tratamento ela aborta o `COPY` inteiro na
violação de chave primária.

```bash
python - <<'PY'
import json, zipfile, os
os.makedirs('tests/fixtures', exist_ok=True)
linhas = [
  ('01310100', 'Avenida Paulista', 'lado ímpar', 'Bela Vista', 'São Paulo', 'SP', '3550308'),
  ('01310100', 'Avenida Paulista', 'lado ímpar', 'Bela Vista', 'São Paulo', 'SP', '3550308'),
  ('01001000', 'Praça da Sé', 'lado par', 'Sé', 'São Paulo', 'SP', '3550308'),
  ('69900001', '', '', '', 'Rio Branco', 'AC', '1200401'),
  ('88010000', 'Rua Felipe Schmidt', '', '', 'Florianópolis', 'SC', '4205407'),
  ('30130000', 'Avenida Afonso Pena, lado ímpar', '', 'Centro', 'Belo Horizonte', 'MG', '3106200'),
  ('40020000', 'Praça da "Sé" Velha', '', 'Centro', 'Salvador', 'BA', '2927408'),
  ('20031000', 'Avenida Rio Branco', 'de 1 a 999', 'Centro', 'Rio de Janeiro', 'RJ', '3304557'),
  ('80010000', 'Rua XV de Novembro', '', 'Centro', 'Curitiba', 'PR', '4106902'),
  ('90010000', 'Rua dos Andradas', '', 'Centro Histórico', 'Porto Alegre', 'RS', '4314902'),
  ('70040010', 'Esplanada dos Ministérios', '', 'Zona Cívico-Administrativa', 'Brasília', 'DF', '5300108'),
  ('66010000', 'Avenida Presidente Vargas', '', 'Campina', 'Belém', 'PA', '1501402'),
]
with zipfile.ZipFile('tests/fixtures/cep-mini.zip', 'w', zipfile.ZIP_DEFLATED) as z:
    for i, (cep, log, comp, bai, loc, uf, ibge) in enumerate(linhas):
        d = {'cep': cep[:5] + '-' + cep[5:], 'logradouro': log, 'complemento': comp,
             'bairro': bai, 'localidade': loc, 'uf': uf, 'ibge': ibge}
        z.writestr(f'v1/{cep}_{i}.json', json.dumps(d, ensure_ascii=False, indent=2))
print('entradas:', len(linhas), 'distintos:', len({l[0] for l in linhas}))
PY
```

Conferir que a saída diz `entradas: 12 distintos: 11`, e que o git trata o
arquivo como binário:

```bash
git check-attr text -- tests/fixtures/cep-mini.zip   # espera: text: unset
```

- [ ] **Step 8: Escrever o teste que falha (leitura do zip)**

Criar `tests/integracao/cep-carregar.test.ts`:

```ts
import { describe, expect, test } from 'vitest'
import { lerZip } from '@/src/server/cep/zip'
import type { LinhaCep } from '@/src/server/cep/linha'

const FIXTURE = 'tests/fixtures/cep-mini.zip'

describe('lerZip', () => {
  test('lê as doze entradas, inclusive a repetida', async () => {
    const lidas: LinhaCep[] = []
    const r = await lerZip(FIXTURE, (l) => lidas.push(l))
    expect(r).toEqual({ lidas: 12, descartadas: 0 })
    expect(lidas).toHaveLength(12)
    expect(new Set(lidas.map((l) => l.cep)).size).toBe(11)
  })

  test('traduz complemento em faixa e vazio em null', async () => {
    const lidas: LinhaCep[] = []
    await lerZip(FIXTURE, (l) => lidas.push(l))
    expect(lidas.find((l) => l.cep === '01001000')).toMatchObject({ faixa: 'lado par' })
    expect(lidas.find((l) => l.cep === '69900001')).toMatchObject({
      logradouro: null, faixa: null, bairro: null, localidade: 'Rio Branco',
    })
  })

  test('acento sobrevive: o arquivo é UTF-8', async () => {
    const lidas: LinhaCep[] = []
    await lerZip(FIXTURE, (l) => lidas.push(l))
    expect(lidas.find((l) => l.cep === '88010000')?.localidade).toBe('Florianópolis')
  })

  test('zip inexistente rejeita, não devolve zero linhas', async () => {
    await expect(lerZip('tests/fixtures/nao-existe.zip', () => {})).rejects.toThrow()
  })
})
```

- [ ] **Step 9: Rodar e ver falhar**

Run: `npm run test:integracao -- cep-carregar`
Expected: FAIL — `Failed to resolve import '@/src/server/cep/zip'`.

- [ ] **Step 10: Implementar `zip.ts`**

Criar `src/server/cep/zip.ts`:

```ts
import yauzl from 'yauzl'
import { linhaDoJson, type LinhaCep } from './linha'

// Lê o zip SEM EXTRAIR. Extrair é o caminho errado: a base real tem 1,2 milhão
// de arquivos de 184 bytes em média, e em NTFS cada um consome um cluster de
// 4 KB — 212 MB de conteúdo viram ~4,6 GB em disco.
export function lerZip(
  caminho: string,
  aoLer: (l: LinhaCep) => void,
): Promise<{ lidas: number; descartadas: number }> {
  return new Promise((resolver, rejeitar) => {
    yauzl.open(caminho, { lazyEntries: true }, (erro, zip) => {
      if (erro || !zip) return rejeitar(erro ?? new Error(`zip não abriu: ${caminho}`))
      let lidas = 0
      let descartadas = 0
      zip.on('error', rejeitar)
      zip.on('end', () => resolver({ lidas, descartadas }))
      zip.on('entry', (entrada) => {
        if (entrada.fileName.endsWith('/')) return zip.readEntry()
        zip.openReadStream(entrada, (erroFluxo, fluxo) => {
          if (erroFluxo || !fluxo) return rejeitar(erroFluxo ?? new Error(`entrada não abriu: ${entrada.fileName}`))
          const pedacos: Buffer[] = []
          fluxo.on('data', (p: Buffer) => pedacos.push(p))
          fluxo.on('error', rejeitar)
          fluxo.on('end', () => {
            const linha = linhaDoJson(Buffer.concat(pedacos).toString('utf8'))
            if (linha) {
              lidas++
              aoLer(linha)
            } else {
              descartadas++
            }
            zip.readEntry()
          })
        })
      })
      zip.readEntry()
    })
  })
}
```

- [ ] **Step 11: Rodar e ver passar**

Run: `npm run test:integracao -- cep-carregar && npm run typecheck && npm run lint`
Expected: PASS nos três.

- [ ] **Step 12: Commit**

```bash
git branch --show-current
git add .gitattributes package.json package-lock.json \
        src/server/cep/linha.ts src/server/cep/linha.test.ts src/server/cep/zip.ts \
        tests/fixtures/cep-mini.zip tests/integracao/cep-carregar.test.ts
git commit -m "cep: leitura do zip sem extrair

Extrair e o caminho errado: 1,2 milhao de arquivos de 184 bytes viram ~4,6 GB
em NTFS por causa do cluster de 4 KB. A parte pura (JSON -> linha, linha ->
CSV) fica separada e testada sem banco; o zip so e exercitado contra o
fixture. O fixture tem um CEP repetido de proposito, porque a base real tem
uma duplicata e sem tratamento ela aborta o COPY na chave primaria.

.gitattributes ganha *.zip -text ANTES de o fixture existir: 'provavelmente o
git faz a coisa certa' foi o que custou o diagnostico errado da R-011."
```

---

### Task 6: O carregador e o script

**Files:**
- Create: `src/server/cep/manifesto.ts`
- Create: `src/server/cep/manifesto.test.ts`
- Create: `src/server/cep/carregar.ts`
- Create: `scripts/db/cep-carregar.mts`
- Create: `db/cep/opencep.json`
- Modify: `tests/integracao/cep-carregar.test.ts`
- Modify: `package.json` (script `db:cep:carregar`)

**Interfaces:**
- Consumes: `lerZip` e `paraCsv` das Tasks 5; `comAdmin` de
  `@/src/server/db/admin`; `urlDe` e `sair` de `scripts/db/env.mts`.
- Produces: `type Manifesto`, `lerManifesto(caminho: string): Manifesto`,
  `somaDoArquivo(caminho: string): Promise<string>`, e
  `carregar(o: { urlAdmin: string; caminhoZip: string; manifesto: Manifesto }): Promise<Resultado>`
  onde `Resultado = { ok: true; linhas: number } | { ok: false; motivo: 'soma_diferente' | 'contagem_diferente'; detalhe: string }`.

- [ ] **Step 1: Escrever o teste que falha (manifesto)**

Criar `src/server/cep/manifesto.test.ts`:

```ts
import { describe, expect, test } from 'vitest'
import { lerManifesto, somaDoArquivo } from './manifesto'

describe('lerManifesto', () => {
  test('lê o manifesto real da base', () => {
    const m = lerManifesto('db/cep/opencep.json')
    expect(m).toEqual({
      fonte: 'opencep',
      versao: '2.0.1',
      publicado_em: '2024-07-08',
      url: 'https://github.com/SeuAliado/OpenCEP/releases/download/2.0.1/v1.zip',
      sha256: 'cffa33783b7cb2bec060acd499eb8356698918efdc79238708aac46fcce9e62d',
      linhas_esperadas: 1209313,
    })
  })

  test('manifesto inexistente lança, não devolve objeto meio vazio', () => {
    expect(() => lerManifesto('db/cep/nao-existe.json')).toThrow()
  })
})

describe('somaDoArquivo', () => {
  test('devolve sha256 em minúsculas, 64 caracteres', async () => {
    const s = await somaDoArquivo('tests/fixtures/cep-mini.zip')
    expect(s).toMatch(/^[0-9a-f]{64}$/)
  })

  test('a mesma soma nas duas chamadas', async () => {
    const [a, b] = await Promise.all([
      somaDoArquivo('tests/fixtures/cep-mini.zip'),
      somaDoArquivo('tests/fixtures/cep-mini.zip'),
    ])
    expect(a).toBe(b)
  })
})
```

- [ ] **Step 2: Rodar e ver falhar**

Run: `npm run test:unit -- manifesto`
Expected: FAIL — módulo não existe.

- [ ] **Step 3: Criar o manifesto real e implementar**

Criar `db/cep/opencep.json`:

```json
{
  "fonte": "opencep",
  "versao": "2.0.1",
  "publicado_em": "2024-07-08",
  "url": "https://github.com/SeuAliado/OpenCEP/releases/download/2.0.1/v1.zip",
  "sha256": "cffa33783b7cb2bec060acd499eb8356698918efdc79238708aac46fcce9e62d",
  "linhas_esperadas": 1209313
}
```

`linhas_esperadas` é a contagem **depois** da deduplicação: o arquivo tem
1.209.314 entradas e 1.209.313 CEPs distintos.

Criar `src/server/cep/manifesto.ts`:

```ts
import { createHash } from 'node:crypto'
import { createReadStream, readFileSync } from 'node:fs'

export type Manifesto = {
  fonte: string
  versao: string
  publicado_em: string
  url: string
  sha256: string
  linhas_esperadas: number
}

// O manifesto é o que transforma "baixei um arquivo da internet" em "carreguei
// o retrato que está registrado". Mesmo padrão do railway-ca.pem: o arquivo
// grande fica fora do repositório, a soma fica dentro.
export function lerManifesto(caminho: string): Manifesto {
  const m = JSON.parse(readFileSync(caminho, 'utf8')) as Manifesto
  const faltando = (['fonte', 'versao', 'publicado_em', 'url', 'sha256', 'linhas_esperadas'] as const).filter(
    (c) => m[c] === undefined,
  )
  if (faltando.length > 0) throw new Error(`manifesto sem ${faltando.join(', ')}: ${caminho}`)
  if (!/^[0-9a-f]{64}$/.test(m.sha256)) throw new Error(`sha256 malformado no manifesto: ${caminho}`)
  return m
}

export function somaDoArquivo(caminho: string): Promise<string> {
  return new Promise((resolver, rejeitar) => {
    const hash = createHash('sha256')
    const fluxo = createReadStream(caminho)
    fluxo.on('error', rejeitar)
    fluxo.on('data', (p) => hash.update(p))
    fluxo.on('end', () => resolver(hash.digest('hex')))
  })
}
```

- [ ] **Step 4: Rodar e ver passar**

Run: `npm run test:unit -- manifesto`
Expected: PASS.

- [ ] **Step 5: Escrever o teste que falha (a carga)**

Acrescentar em `tests/integracao/cep-carregar.test.ts`. **Atenção:** o arquivo
já importa `{ describe, expect, test }` de `vitest` desde a Task 5 — juntar
`afterAll` e `beforeAll` naquela linha, não criar um segundo `import` do mesmo
módulo.

```ts
// a linha de import do vitest passa a ser:
// import { afterAll, beforeAll, describe, expect, test } from 'vitest'
import { carregar } from '@/src/server/cep/carregar'
import { somaDoArquivo, type Manifesto } from '@/src/server/cep/manifesto'
import { criarBancoDeTeste, type BancoDeTeste } from './ajuda'

let banco: BancoDeTeste
let manifesto: Manifesto

beforeAll(async () => {
  banco = await criarBancoDeTeste()
  manifesto = {
    fonte: 'fixture',
    versao: 'mini',
    publicado_em: '2024-07-08',
    url: 'file://tests/fixtures/cep-mini.zip',
    // A soma é calculada aqui, não colada: o zip é construído localmente e o
    // valor mudaria a cada reconstrução. O caminho vermelho usa soma errada.
    sha256: await somaDoArquivo(FIXTURE),
    linhas_esperadas: 11,
  }
})
afterAll(async () => {
  await banco.derrubar()
})

describe('carregar', () => {
  test('carrega o fixture, deduplica e grava o retrato', async () => {
    const r = await carregar({ urlAdmin: banco.urlAdmin, caminhoZip: FIXTURE, manifesto })
    expect(r).toEqual({ ok: true, linhas: 11 })

    const linhas = await banco.sql<{ n: string }>('SELECT count(*)::text AS n FROM cep')
    expect(linhas).toEqual([{ n: '11' }])

    const carga = await banco.sql<{ fonte: string; versao: string; linhas: number }>(
      'SELECT fonte, versao, linhas FROM cep_carga',
    )
    expect(carga).toEqual([{ fonte: 'fixture', versao: 'mini', linhas: 11 }])
  })

  test('a duplicata não aborta a carga: 12 entradas viram 11 linhas', async () => {
    const r = await banco.sql<{ n: string }>("SELECT count(*)::text AS n FROM cep WHERE cep = '01310100'")
    expect(r).toEqual([{ n: '1' }])
  })

  test('vírgula e aspas atravessam o CSV sem estragar a linha', async () => {
    const r = await banco.sql<{ logradouro: string }>(
      "SELECT logradouro FROM cep WHERE cep IN ('30130000', '40020000') ORDER BY cep",
    )
    expect(r).toEqual([
      { logradouro: 'Avenida Afonso Pena, lado ímpar' },
      { logradouro: 'Praça da "Sé" Velha' },
    ])
  })

  test('carregar de novo substitui o retrato inteiro, sem duplicar', async () => {
    await carregar({ urlAdmin: banco.urlAdmin, caminhoZip: FIXTURE, manifesto })
    const linhas = await banco.sql<{ n: string }>('SELECT count(*)::text AS n FROM cep')
    expect(linhas).toEqual([{ n: '11' }])
    const cargas = await banco.sql<{ n: string }>('SELECT count(*)::text AS n FROM cep_carga')
    expect(cargas).toEqual([{ n: '2' }])
  })

  test('soma diferente para antes de tocar no banco', async () => {
    const errado = { ...manifesto, sha256: '0'.repeat(64) }
    const r = await carregar({ urlAdmin: banco.urlAdmin, caminhoZip: FIXTURE, manifesto: errado })
    expect(r).toMatchObject({ ok: false, motivo: 'soma_diferente' })
    const linhas = await banco.sql<{ n: string }>('SELECT count(*)::text AS n FROM cep')
    expect(linhas).toEqual([{ n: '11' }])
  })

  test('contagem diferente faz ROLLBACK e o retrato anterior sobrevive', async () => {
    const errado = { ...manifesto, versao: 'quebrada', linhas_esperadas: 999 }
    const r = await carregar({ urlAdmin: banco.urlAdmin, caminhoZip: FIXTURE, manifesto: errado })
    expect(r).toMatchObject({ ok: false, motivo: 'contagem_diferente' })
    const linhas = await banco.sql<{ n: string }>('SELECT count(*)::text AS n FROM cep')
    expect(linhas).toEqual([{ n: '11' }])
    const quebrada = await banco.sql<{ n: string }>(
      "SELECT count(*)::text AS n FROM cep_carga WHERE versao = 'quebrada'",
    )
    expect(quebrada).toEqual([{ n: '0' }])
  })
})
```

- [ ] **Step 6: Rodar e ver falhar**

Run: `npm run test:integracao -- cep-carregar`
Expected: FAIL — `carregar` não existe.

- [ ] **Step 7: Implementar `carregar.ts`**

Criar `src/server/cep/carregar.ts`:

```ts
import { pipeline } from 'node:stream/promises'
import { Readable } from 'node:stream'
import { from as copiarDe } from 'pg-copy-streams'
import { comAdmin } from '../db/admin'
import { paraCsv } from './linha'
import { somaDoArquivo, type Manifesto } from './manifesto'
import { lerZip } from './zip'

export type Resultado =
  | { ok: true; linhas: number }
  | { ok: false; motivo: 'soma_diferente' | 'contagem_diferente'; detalhe: string }

const COLUNAS = '(cep, logradouro, faixa, bairro, localidade, uf, ibge)'

export async function carregar(o: {
  urlAdmin: string
  caminhoZip: string
  manifesto: Manifesto
}): Promise<Resultado> {
  // Catraca 1, antes de abrir transação: o arquivo em disco é o que o
  // manifesto registra? Diferente, para — não avisa e continua.
  const soma = await somaDoArquivo(o.caminhoZip)
  if (soma !== o.manifesto.sha256) {
    return { ok: false, motivo: 'soma_diferente', detalhe: `esperado ${o.manifesto.sha256}, lido ${soma}` }
  }

  const csv: string[] = []
  await lerZip(o.caminhoZip, (l) => csv.push(paraCsv(l)))

  return comAdmin(o.urlAdmin, async (c) => {
    await c.query('BEGIN')
    try {
      // TRUNCATE é transacional no Postgres, então nunca existe um instante em
      // que a aplicação veja a tabela vazia: ou o retrato novo entra inteiro,
      // ou o antigo continua.
      await c.query('TRUNCATE cep')
      // Temporária SEM chave: a base real tem uma duplicata, e COPY direto na
      // PK abortaria a carga inteira. O DISTINCT ON abaixo resolve no banco,
      // sem um Set de 1,2 milhão de strings na memória do Node.
      await c.query(`CREATE TEMP TABLE cep_bruto (LIKE cep) ON COMMIT DROP`)
      await c.query('ALTER TABLE cep_bruto DROP CONSTRAINT IF EXISTS cep_bruto_pkey')

      const destino = c.query(copiarDe(`COPY cep_bruto ${COLUNAS} FROM STDIN WITH (FORMAT csv)`))
      await pipeline(Readable.from(csv), destino)

      await c.query(`INSERT INTO cep ${COLUNAS}
        SELECT DISTINCT ON (cep) cep, logradouro, faixa, bairro, localidade, uf, ibge
          FROM cep_bruto ORDER BY cep`)

      // Catraca 2, DENTRO da transação: carga truncada por erro de leitura não
      // fica de pé. O ROLLBACK devolve o retrato anterior.
      const { rows } = await c.query<{ n: string }>('SELECT count(*)::text AS n FROM cep')
      const linhas = Number(rows[0].n)
      if (linhas !== o.manifesto.linhas_esperadas) {
        await c.query('ROLLBACK')
        return {
          ok: false,
          motivo: 'contagem_diferente',
          detalhe: `esperado ${o.manifesto.linhas_esperadas}, carregado ${linhas}`,
        }
      }

      await c.query(
        `INSERT INTO cep_carga (fonte, versao, publicado_em, arquivo_sha256, linhas)
         VALUES ($1, $2, $3, $4, $5)`,
        [o.manifesto.fonte, o.manifesto.versao, o.manifesto.publicado_em, o.manifesto.sha256, linhas],
      )
      await c.query('COMMIT')
      return { ok: true, linhas }
    } catch (erro) {
      await c.query('ROLLBACK')
      throw erro
    }
  })
}
```

Nota sobre `CREATE TEMP TABLE cep_bruto (LIKE cep)`: `LIKE` sem `INCLUDING
CONSTRAINTS` **não** copia a chave primária, mas copia os `CHECK` e os `NOT
NULL`, que é o que queremos — dado malformado ainda é recusado. O `ALTER TABLE
... DROP CONSTRAINT IF EXISTS` é cinto de segurança para o caso de o `LIKE`
mudar de comportamento; se ele falhar por a restrição não existir, o `IF EXISTS`
já cobre.

- [ ] **Step 8: Rodar e ver passar**

Run: `npm run test:integracao -- cep-carregar`
Expected: PASS, os seis testes.

- [ ] **Step 9: Escrever o CLI**

Criar `scripts/db/cep-carregar.mts`:

```ts
import { carregar } from '../../src/server/cep/carregar'
import { lerManifesto } from '../../src/server/cep/manifesto'
import { sair, urlDe } from './env.mts'

const [caminhoZip, caminhoManifesto = 'db/cep/opencep.json'] = process.argv.slice(2)
if (!caminhoZip) {
  sair(false, 'uso: npm run db:cep:carregar -- <caminho-do-zip> [caminho-do-manifesto]')
}

const url = urlDe('DATABASE_URL_ADMIN')
const manifesto = lerManifesto(caminhoManifesto)
console.log(`host: ${new URL(url).host}`)
console.log(`base: ${manifesto.fonte} ${manifesto.versao}, publicada em ${manifesto.publicado_em}`)

const inicio = Date.now()
const r = await carregar({ urlAdmin: url, caminhoZip, manifesto })
const segundos = ((Date.now() - inicio) / 1000).toFixed(1)

if (!r.ok) sair(false, `não carregado (${r.motivo}): ${r.detalhe}`)
sair(true, `carregadas ${r.linhas} linhas em ${segundos}s`)
```

Acrescentar em `package.json`, na seção `scripts`, depois de `db:senha`:

```json
    "db:cep:carregar": "tsx scripts/db/cep-carregar.mts"
```

- [ ] **Step 10: Provar o CLI contra o fixture**

O manifesto real não bate com o fixture, então este passo prova o caminho
vermelho do CLI — que é o que queremos ver funcionando antes da carga de verdade:

```bash
npm run db:cep:carregar -- tests/fixtures/cep-mini.zip
```

Expected: sai com código 1 e a mensagem
`não carregado (soma_diferente): esperado cffa3378..., lido ...`.

- [ ] **Step 11: Verificar o conjunto**

Run: `npm run typecheck && npm run lint && npm run test`
Expected: PASS nos três.

- [ ] **Step 12: Commit**

```bash
git branch --show-current
git add src/server/cep/manifesto.ts src/server/cep/manifesto.test.ts \
        src/server/cep/carregar.ts scripts/db/cep-carregar.mts \
        db/cep/opencep.json tests/integracao/cep-carregar.test.ts package.json
git commit -m "cep: carregador com as duas catracas

sha256 conferido antes de abrir transacao; contagem conferida dentro dela,
com ROLLBACK que devolve o retrato anterior. TRUNCATE e transacional, entao
a aplicacao nunca ve a tabela vazia.

Temporaria sem chave mais DISTINCT ON: a base real tem uma duplicata e COPY
direto na PK abortaria a carga inteira. Deduplicar no banco evita um Set de
1,2 milhao de strings na memoria do Node."
```

---

### Task 7: A carga de verdade e a verificação manual

Esta é a única task sem teste automático, porque o que ela verifica é o arquivo
real de 326 MB contra o banco real. Segue o formato das fatias 0b e 0c.

**Files:**
- Modify: `docs/db/divida-tecnica.md`
- Modify: `docs/db/fundacao.md`

**Interfaces:**
- Consumes: tudo das Tasks 1 a 6.
- Produces: nada de código. Produz a linha de `cep_carga` no container e o
  registro da verificação.

- [ ] **Step 1: Carregar a base real no container**

```bash
npm run db:aplicar
npm run db:cep:carregar -- "C:/Users/ADM/projetos/.bases/opencep-2.0.1.zip"
```

Anotar o tempo que o comando imprime. Expected: `carregadas 1209313 linhas em Ns`.

**Se a conversão passar de 3 minutos**, aplicar a tabela de decisão da spec
(seção "Tempo de conversão"): de 3 a 10 minutos o script ganha impressão de
progresso; acima de 10 minutos ele se divide em `db:cep:preparar` (gera o CSV) e
`db:cep:carregar` (só o `COPY`). Isso vira task nova, não remendo aqui.

- [ ] **Step 2: Conferir o resultado**

```bash
docker exec -i crm-postgres psql -U postgres -d crm -c "
SELECT count(*) AS linhas,
       pg_size_pretty(pg_total_relation_size('cep')) AS tamanho FROM cep;
SELECT fonte, versao, publicado_em, linhas FROM cep_carga;
SELECT logradouro, bairro, localidade, uf FROM cep WHERE cep = '01310100';"
```

Expected: 1.209.313 linhas, ~156 MB, uma linha em `cep_carga` com
`opencep 2.0.1 2024-07-08 1209313`, e o CEP `01310100` devolvendo Avenida
Paulista, São Paulo, SP.

**O CEP de teste é `01310100` de propósito.** Com a base parada em julho de 2024,
testar com um CEP recente cai em não encontrado e **parece bug numa carga que
funcionou**. `01310100` foi resolvido no spike contra a base real, então é sabido
que está lá. Quando um CEP não resolve, a primeira hipótese a descartar é a idade
do endereço, não o defeito do código.

- [ ] **Step 3: Registrar a verificação manual**

Acrescentar em `docs/db/divida-tecnica.md`, antes da seção
`## Fonte externa: a base de CEP do OpenCEP`:

```markdown
## Fatia cep: verificado à mão, sem teste automático

A carga completa não roda em teste — o automático usa `tests/fixtures/cep-mini.zip`,
com doze entradas. O que garante o arquivo real de 326 MB é a verificação abaixo,
feita contra o container local. Refazer quando mexer em `src/server/cep/**` ou em
`scripts/db/cep-carregar.mts`.

| Caminho | Verificado |
|---|---|
| `db:cep:carregar` com o zip real termina com 1.209.313 linhas | |
| `cep` ocupa ~156 MB (tabela + índice) | |
| `cep_carga` tem uma linha: `opencep 2.0.1 2024-07-08` | |
| `01310100` resolve para Avenida Paulista, São Paulo, SP | |
| soma errada no manifesto para antes de tocar no banco | |
| rodar de novo mantém 1.209.313 linhas e acrescenta uma linha em `cep_carga` | |

Tempo real da carga: **preencher**.

**O CEP de teste é `01310100`, não o primeiro que vier à cabeça.** A base está
parada em julho de 2024; um CEP recente cai em não encontrado e parece bug numa
carga que funcionou.
```

Preencher a coluna "Verificado" com `sim` conforme cada linha for conferida, e
substituir "preencher" pelo tempo medido no Step 1.

- [ ] **Step 4: Apontar a fundação para a fatia nova**

Acrescentar em `docs/db/fundacao.md`, no fim da seção "Feito na fatia 0b (login e
sessão)", como parágrafo novo:

```markdown
Feito na fatia `cep`: `cep` e `cep_carga` (0013), a primeira tabela do projeto
com leitura irrestrita (`USING (true)`, justificada em `docs/db/0013.md`), a
invariante `POLITICAS_DE_LEITURA_IRRESTRITA` que a cataloga, e o carregador
operacional `db:cep:carregar`, que escreve com `DATABASE_URL_ADMIN` porque
`app_usuario` só tem `SELECT`. Desenho em
`docs/superpowers/specs/2026-09-09-cep-design.md`.
```

- [ ] **Step 5: Rodar tudo uma última vez**

Run: `npm run db:checar && npm run typecheck && npm run lint && npm run test`
Expected: PASS nos quatro.

- [ ] **Step 6: Commit e PR**

```bash
git branch --show-current
git add docs/db/divida-tecnica.md docs/db/fundacao.md
git commit -m "cep: verificacao manual da carga real registrada

O automatico usa um fixture de doze entradas; o arquivo real de 326 MB e
conferido a mao, no formato das fatias 0b e 0c. CEP de teste fixado em
01310100 porque a base parada em julho de 2024 faz CEP recente parecer bug."
git push -u origin fatia-cep
gh pr create --fill
```

Merge só com CI verde (R-001), e a proteção da `main` agora tem
`enforce_admins` ligado (R-013), então ela vale de verdade.

---

## Notas para quem executa

**A ordem das tasks 1 e 2 é deliberada.** A invariante nasce com a lista vazia
para ser verde contra o banco de hoje; a primeira entrada entra no mesmo commit
que cria a tabela que a justifica. Inverter a ordem quebra o `db:aplicar` entre
os dois commits.

**A duplicata não é hipótese, é medição.** O arquivo real tem 1.209.314 entradas
e 1.209.313 CEPs distintos. Sem a temporária e o `DISTINCT ON`, a carga aborta
com violação de chave primária na única linha repetida — depois de já ter gasto
o tempo inteiro da conversão.

**A carga em produção não faz parte desta fatia.** O plano carrega no container
local e para aí. Quando a Railway for carregada, o pré-requisito da spec vale e
é humano, não automatizável: **conferir o espaço no painel da Railway antes de
rodar**. O banco tem 8 MB hoje e a carga acrescenta 156 MB — 3% do volume no
plano Hobby, 31% no Free/Trial, e o plano não foi confirmado. O comando é
`ALVO=railway npm run db:cep:carregar -- <zip>`, e o script imprime o host antes
de escrever justamente para dar a chance de abortar.

**Se um número medido no spike não bater**, não ajustar o teste para o valor
novo sem entender. Os números vieram do arquivo `cffa3378…9e62d`; se o zip for
outro, a catraca do sha256 para antes, que é o comportamento certo.
