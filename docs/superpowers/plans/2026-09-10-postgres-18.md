# Plano: Postgres 18

Base: spec 2026-09-10-postgres-18-design.md. Execução nesta conversa.

1. Criar tests/integracao/versao-postgres.test.ts: conectar ao servidor local
   com comAdmin, consultar SHOW server_version_num, comparar a divisão inteira
   por 10000 com Number(process.env.PG_VERSAO_ESPERADA). Pular se ausente.
   Exigir host local antes da conexão. Ver controle negativo em PG17 com 18 esperado.
2. Alterar .github/workflows/ci.yml: postgres:18 e PG_VERSAO_ESPERADA: '18'.
3. Subir container postgres:18 descartável em porta local separada. Rodar a
   suíte com DATABASE_URL_ADMIN apontando para ele e PG_SSL=off; medir versão.
4. Rodar typecheck, lint, db:checar. Atualizar docs/db/divida-tecnica.md com
   o alcance da validação e referências. Revisar diff, abrir PR, aguardar CI
   incluindo build e Playwright, integrar somente com CI verde.
5. Remover apenas o container criado para esta validação.
