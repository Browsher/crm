# Fatia `contato` Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** O vendedor registra o que aconteceu na ligação e marca o próximo passo com data, e o histórico atravessa a troca de dono.

**Architecture:** Uma tabela de eventos (`contato`) pendurada em `empresa`, imutável, lida por uma política que delega para `empresa_leitura`. Uma função definidora (`contato_registrar`) grava o contato e aplica o desfecho da fila na mesma transação, chamando `empresa_assumir`/`empresa_devolver` por dentro — as duas perdem o `GRANT` e viram internas. O próximo passo é derivado do contato mais recente, sem cache.

**Tech Stack:** Postgres 17 (container) / 18.6 (Railway), Next.js 16.3.4 App Router, React 19.2.8, TypeScript, Vitest 5, `pg`.

**Spec:** `docs/superpowers/specs/2026-09-10-contato-design.md` — leia antes da primeira tarefa. O plano argumenta a partir dela; os dois viajam juntos.

## Global Constraints

- **TDD, vermelho primeiro, sempre.** Implementação escrita antes de o teste falhar é motivo de refazer o vermelho.
- **TypeScript sem ponto e vírgula, aspas simples.** Retorno cedo em vez de `else` aninhado.
- **Regra de negócio devolve `{ ok, motivo }`, não `throw`.** Exceção é falha de infraestrutura ou defeito nosso.
- **`src/features/contato/` não importa de `src/features/fila/` nem de `src/features/empresas/`.** `app/` pode importar de qualquer feature. Regra do `CLAUDE.md`.
- **Nenhuma constante de prazo em TypeScript.** Nem 30 minutos, nem 30 dias. Os instantes vêm do banco.
- **Nenhuma comparação de data em TypeScript.** "Vencido" é calculado no banco com `(now() AT TIME ZONE 'America/Sao_Paulo')::date`.
- **Migração é imutável depois de aplicada** (R-011). Arquivo `.sql` fechado numa tarefa não é editado na seguinte. É por isso que esta fatia tem três migrações e não uma.
- **Nada de caractere de controle em prosa** (R-020): `docs/**/*.md` e `.md` da raiz passam por `src/docs.test.ts`.
- **Branch e PR contra a `main`, sem empilhar** (R-021). A branch desta fatia é `fatia-contato-spec`, que já existe e já tem a spec commitada.
- **Confira `git branch --show-current` antes de cada commit** — a conferência informa, não barra (R-013). Se imprimir `main`, pare.
- **Números fixados nesta fatia, os dois rotulados como chute:** `7` dias (gatilho C, próximo passo vencido) e `80` empresas (gatilho D, tamanho de carteira).
- **O vocabulário de tipo é fechado em TypeScript, e o banco só exige não-vazio.** A única defesa é teste.

## Por que três migrações e não uma

A spec diz `0017_contato.sql` com tabela, função e revogações juntas. O plano separa em três arquivos, e o motivo é a R-011: `db:aplicar` grava a soma do arquivo, e migração editada depois de aplicada acusa divergência. Com um arquivo só, as tarefas 1, 4 e 5 editariam o mesmo `.sql` depois de ele já ter rodado no banco de desenvolvimento.

Separadas, cada uma é aplicável sozinha e deixa o sistema verde:

| Arquivo | O que entra | Tarefa |
|---|---|---|
| `0017_contato.sql` | tabela, índice, gatilho de auditoria, RLS, política, `GRANT SELECT` | 1 |
| `0018_contato_registrar.sql` | a função definidora e o `GRANT` dela | 4 |
| `0019_fila_interna.sql` | `REVOKE` de `empresa_assumir` e `empresa_devolver` | 5 |

A 0019 é separada da 0018 de propósito: um revisor pode aceitar a função e recusar a revogação, e essa é exatamente a decisão mais discutível da fatia.

## Estrutura de arquivos

**Criar:**

| Arquivo | Responsabilidade |
|---|---|
| `db/migracoes/0017_contato.sql` | a tabela e a leitura |
| `db/migracoes/0018_contato_registrar.sql` | a escrita |
| `db/migracoes/0019_fila_interna.sql` | fechar as duas funções da fila |
| `src/features/contato/tipos.ts` | a lista fechada, rótulos e desfecho sugerido |
| `src/features/contato/regras.ts` | validação pura do rascunho de contato |
| `src/features/contato/repositorio.ts` | `contato_registrar` e tradução do vocabulário |
| `src/features/contato/mensagens.ts` | motivo para texto de tela |
| `src/features/contato/historico.ts` | a linha do tempo de uma empresa |
| `src/features/contato/acao.ts` | a action única, `'use server'` |
| `src/features/contato/formulario.tsx` | o formulário compartilhado, componente cliente |
| `src/features/contato/linha-do-tempo.tsx` | render da linha do tempo |
| `app/carteira/[id]/page.tsx` | a ficha |
| `tests/integracao/contato-politicas.test.ts` | privilégios e herança de leitura |
| `tests/integracao/contato-registrar.test.ts` | a função, os desfechos e a corrida |

**Modificar:**

| Arquivo | O quê |
|---|---|
| `src/features/fila/consulta.ts` | próximo passo derivado e `vencido` em `EmpresaComigo`; ordem da carteira |
| `src/server/db/migracoes/invariantes.ts:23-34` | a lista de funções concedidas |
| `src/server/diretivas.test.ts:23` | o glob passa a incluir `src/**` |
| `app/fila/cartao.tsx` | histórico e formulário no cartão |
| `app/fila/formulario.tsx` | passa a montar o formulário de contato |
| `app/fila/acoes.ts` | perde `assumir` e `devolver`; sobra `puxar` |
| `app/carteira/lista.tsx` | agenda ordenada, grupo "sem próximo passo" contado, link para a ficha |
| `app/carteira/page.tsx` | passa o que a lista precisa |
| `tests/integracao/fila-posse.test.ts` | chamadas diretas passam por `contato_registrar` |
| `app/fila/cartao.test.tsx` | os campos novos de `EmpresaComigo` |
| `app/carteira/lista.test.tsx` | os campos novos e a ordem |

---

### Task 1: Migração 0017 — a tabela `contato` e a política de leitura

**Files:**
- Create: `db/migracoes/0017_contato.sql`
- Create: `tests/integracao/contato-politicas.test.ts`

**Interfaces:**
- Consumes: `empresa`, `empresa_fila`, `usuario`, `definir_auditoria()` (0003), `empresa_leitura` (0016).
- Produces: tabela `contato` com colunas `id, empresa_id, tipo, nota, proximo_passo, proximo_passo_data, criado_em, criado_por, atualizado_em, atualizado_por`; política `contato_leitura`; `GRANT SELECT ON contato TO app_usuario`.

- [ ] **Step 1: Escreva o teste de privilégios e herança de leitura**

Crie `tests/integracao/contato-politicas.test.ts`. O molde é `tests/integracao/fila-politicas.test.ts` — abra-o antes e siga a forma dos helpers e do `beforeAll`.

```ts
import { afterAll, beforeAll, beforeEach, describe, expect, test } from 'vitest'
import { conectarVerificado } from '@/src/server/db/pool'
import { criarBancoDeTeste, criarUsuario, type BancoDeTeste } from './ajuda'

let banco: BancoDeTeste
let gestor: string
let vendedorA: string
let vendedorB: string
let empresa: string

beforeAll(async () => {
  banco = await criarBancoDeTeste()
  gestor = await criarUsuario(banco, 'gestor', 'Gestor')
  vendedorA = await criarUsuario(banco, 'vendedor', 'VendedorA')
  vendedorB = await criarUsuario(banco, 'vendedor', 'VendedorB')
  const [linha] = await banco.sql<{ id: string }>(
    `INSERT INTO empresa (cnpj, razao_social, telefone)
     VALUES ('11222333000181', 'Aurora Comercio LTDA', '11987654321') RETURNING id`,
  )
  empresa = linha.id
})
afterAll(async () => {
  await banco.derrubar()
})

beforeEach(async () => {
  await banco.sql('DELETE FROM contato')
  await banco.sql('DELETE FROM empresa_fila')
})

// Cenário escrito como dona: a RLS não vale aqui, e é isso que faz disto
// preparação e não exercício do desenho.
async function contatoDe(autor: string, tipo = 'nao_atendeu'): Promise<string> {
  const [linha] = await banco.sql<{ id: string }>(
    'INSERT INTO contato (empresa_id, tipo, criado_por) VALUES ($1, $2, $3) RETURNING id',
    [empresa, tipo, autor],
  )
  return linha.id
}

async function reservarPara(usuarioId: string, minutos = 30): Promise<void> {
  await banco.sql(
    `INSERT INTO empresa_fila (empresa_id, reservado_por, reservado_ate, primeira_reserva_em)
     VALUES ($1, $2, now() + ($3 || ' minutes')::interval, now())
     ON CONFLICT (empresa_id) DO UPDATE
        SET reservado_por = excluded.reservado_por,
            reservado_ate = excluded.reservado_ate,
            vendedor_id = NULL`,
    [empresa, usuarioId, String(minutos)],
  )
}

async function darPosseA(usuarioId: string): Promise<void> {
  await banco.sql(
    `INSERT INTO empresa_fila (empresa_id, vendedor_id, primeira_reserva_em)
     VALUES ($1, $2, now())
     ON CONFLICT (empresa_id) DO UPDATE
        SET vendedor_id = excluded.vendedor_id, reservado_por = NULL, reservado_ate = NULL`,
    [empresa, usuarioId],
  )
}

function lerContatos(usuarioId: string): Promise<{ id: string }[]> {
  return banco.comoUsuario(usuarioId, async (e) => {
    const r = await e<{ id: string }>('SELECT id FROM contato')
    return r.linhas
  })
}

describe('contato: privilégios', () => {
  test('app_conexao fora de comoUsuario e 42501, nao zero linhas', async () => {
    await contatoDe(vendedorA)
    const c = await conectarVerificado(banco.urlApp)
    try {
      await expect(c.query('SELECT id FROM contato')).rejects.toMatchObject({ code: '42501' })
    } finally {
      c.release()
    }
  })

  // A imutabilidade da tabela é por AUSÊNCIA de função de escrita. Ausência
  // não é verificável olhando o código: precisa destes três.
  const escritas: [string, string][] = [
    ['INSERT', "INSERT INTO contato (empresa_id, tipo) VALUES ('11111111-1111-4111-8111-111111111111', 'x')"],
    ['UPDATE', "UPDATE contato SET nota = 'alterada'"],
    ['DELETE', 'DELETE FROM contato'],
  ]
  for (const [verbo, sql] of escritas) {
    test(`${verbo} e 42501 por falta de GRANT, inclusive para o gestor`, async () => {
      await expect(banco.comoUsuario(gestor, (e) => e(sql))).rejects.toMatchObject({ code: '42501' })
    })
  }
})

describe('contato_leitura: segue a empresa, nao o autor', () => {
  test('vendedor com posse le os contatos da empresa', async () => {
    await contatoDe(vendedorA)
    await darPosseA(vendedorA)
    expect(await lerContatos(vendedorA)).toHaveLength(1)
  })

  test('vendedor com reserva vigente le os contatos da empresa', async () => {
    await contatoDe(vendedorA)
    await reservarPara(vendedorA)
    expect(await lerContatos(vendedorA)).toHaveLength(1)
  })

  test('vendedor sem a empresa NAO le contato nenhum', async () => {
    await contatoDe(vendedorA)
    await darPosseA(vendedorA)
    expect(await lerContatos(vendedorB)).toEqual([])
  })

  // O propósito da fatia inteira: o histórico atravessa a troca de dono.
  test('vendedor B, ao ficar com a empresa, le o contato escrito por A', async () => {
    await contatoDe(vendedorA)
    await reservarPara(vendedorB)
    expect(await lerContatos(vendedorB)).toHaveLength(1)
  })

  // Decidido, não defeito: a política segue a empresa. Ver a fragilidade
  // registrada na spec — é a que mais provavelmente vira reclamação.
  test('o AUTOR deixa de ler o proprio contato quando a empresa sai da mao dele', async () => {
    await contatoDe(vendedorA)
    await darPosseA(vendedorB)
    expect(await lerContatos(vendedorA)).toEqual([])
  })

  test('reserva expirada nao da leitura', async () => {
    await contatoDe(vendedorA)
    await reservarPara(vendedorA, -1)
    expect(await lerContatos(vendedorA)).toEqual([])
  })

  test('gestor le tudo, sem posse nenhuma', async () => {
    await contatoDe(vendedorA)
    expect(await lerContatos(gestor)).toHaveLength(1)
  })
})

// Quem PEGA o estreitamento é o teste de comportamento acima, em `contato`.
// Quem EXPLICA é este: nomeado pela dependência, ele falha junto e diz de qual
// tabela veio a causa. Sozinho, o primeiro acusa `contato` quando o problema
// está em `empresa_leitura` ou em `empresa_fila_leitura`.
describe('contato_leitura depende de empresa_leitura, que depende de empresa_fila_leitura', () => {
  test('a empresa reservada e visivel para o vendedor, e e disso que contato_leitura vive', async () => {
    await reservarPara(vendedorA)
    const linhas = await banco.comoUsuario(vendedorA, async (e) => {
      const r = await e<{ id: string }>('SELECT id FROM empresa')
      return r.linhas
    })
    expect(linhas).toHaveLength(1)
  })
})

describe('contato_proximo_passo_coerente', () => {
  test('passo sem data e recusado', async () => {
    await expect(
      banco.sql("INSERT INTO contato (empresa_id, tipo, proximo_passo) VALUES ($1, 'acompanhamento', 'ligar')", [empresa]),
    ).rejects.toMatchObject({ code: '23514' })
  })

  test('data sem passo e recusada', async () => {
    await expect(
      banco.sql("INSERT INTO contato (empresa_id, tipo, proximo_passo_data) VALUES ($1, 'acompanhamento', '2026-10-01')", [empresa]),
    ).rejects.toMatchObject({ code: '23514' })
  })
})

describe('auditoria', () => {
  test('criado_por sai de usuario_atual, nao de parametro', async () => {
    await reservarPara(vendedorA)
    // Inserção como dona não tem identidade; este teste confere o gatilho pela
    // via que a aplicação usa não existe ainda, então checamos o DEFAULT.
    const [linha] = await banco.sql<{ criado_em: Date }>(
      'INSERT INTO contato (empresa_id, tipo) VALUES ($1, $2) RETURNING criado_em',
      [empresa, 'nao_atendeu'],
    )
    expect(linha.criado_em).toBeInstanceOf(Date)
  })
})
```

- [ ] **Step 2: Rode e veja falhar**

```
npm run db:subir
npm run test:integracao -- contato-politicas
```

Esperado: falha na aplicação das migrações ou `relation "contato" does not exist`.

- [ ] **Step 3: Escreva a migração**

Crie `db/migracoes/0017_contato.sql`:

```sql
-- ver docs/db/0017.md
BEGIN;
CREATE TABLE contato (
  id         uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  empresa_id uuid NOT NULL REFERENCES empresa (id) ON DELETE RESTRICT,
  tipo       text NOT NULL CHECK (btrim(tipo) <> ''),
  nota       text CHECK (btrim(nota) <> ''),
  proximo_passo      text CHECK (btrim(proximo_passo) <> ''),
  proximo_passo_data date,
  criado_em      timestamptz NOT NULL DEFAULT now(),
  criado_por     uuid REFERENCES usuario (id) ON DELETE RESTRICT,
  atualizado_em  timestamptz,
  atualizado_por uuid REFERENCES usuario (id) ON DELETE RESTRICT,
  -- Passo e data são um par. Sem isto, "próximo passo sem data" entraria pela
  -- função e sumiria da agenda em silêncio.
  CONSTRAINT contato_proximo_passo_coerente
    CHECK ((proximo_passo IS NULL) = (proximo_passo_data IS NULL))
);
CREATE TRIGGER contato_auditoria BEFORE INSERT OR UPDATE ON contato
FOR EACH ROW EXECUTE FUNCTION definir_auditoria();
-- Serve a linha do tempo da ficha e a busca do contato mais recente por
-- empresa: é a mesma ordem.
CREATE INDEX contato_empresa_idx ON contato (empresa_id, criado_em DESC);
ALTER TABLE contato ENABLE ROW LEVEL SECURITY;
-- "Você lê o contato da empresa que você lê." A RLS de `empresa` é avaliada
-- dentro desta subconsulta, então gestor, posse e reserva vigente entram todos
-- por `empresa_leitura`, sem repetir a lógica da fila aqui.
CREATE POLICY contato_leitura ON contato FOR SELECT
  USING (EXISTS (SELECT 1 FROM empresa e WHERE e.id = contato.empresa_id));
-- SELECT e mais nada: a tabela é imutável por ausência de função de escrita.
GRANT SELECT ON contato TO app_usuario;
COMMIT;
```

- [ ] **Step 4: Rode e veja passar**

```
npm run db:checar
npm run test:integracao -- contato-politicas
```

Esperado: PASS em todos.

- [ ] **Step 5: Commit**

```bash
git branch --show-current
git add db/migracoes/0017_contato.sql tests/integracao/contato-politicas.test.ts
git commit -m "feat(db): 0017 — tabela contato e a política que delega para empresa_leitura"
```

---

### Task 2: `src/features/contato/tipos.ts` — a lista fechada

**Files:**
- Create: `src/features/contato/tipos.ts`
- Test: `src/features/contato/tipos.test.ts`

**Interfaces:**
- Produces: `TIPOS_CONTATO` (tupla `as const`), `type TipoContato`, `type Desfecho = 'nenhum' | 'assumir' | 'devolver'`, `DESFECHO_SUGERIDO: Record<TipoContato, Desfecho>`, `ROTULO: Record<TipoContato, string>`, `ehTipo(v: string): v is TipoContato`, `EXIGE_POSSE: Record<TipoContato, boolean>`.

- [ ] **Step 1: Escreva o teste**

Crie `src/features/contato/tipos.test.ts`:

```ts
import { describe, expect, test } from 'vitest'
import { DESFECHO_SUGERIDO, EXIGE_POSSE, ROTULO, TIPOS_CONTATO, ehTipo } from './tipos'

describe('TIPOS_CONTATO', () => {
  test('tem os seis tipos da spec, e nenhum a mais', () => {
    expect([...TIPOS_CONTATO]).toEqual([
      'nao_liguei',
      'nao_atendeu',
      'retornar_depois',
      'sem_interesse',
      'interessado',
      'acompanhamento',
    ])
  })

  test('todo tipo tem rotulo e desfecho sugerido', () => {
    for (const t of TIPOS_CONTATO) {
      expect(ROTULO[t]).toBeTruthy()
      expect(['nenhum', 'assumir', 'devolver']).toContain(DESFECHO_SUGERIDO[t])
    }
  })

  // O único tipo de posse. Se algum dia outro entrar, este teste obriga a
  // decidir conscientemente em vez de deixar acontecer.
  test('acompanhamento e o unico tipo de posse', () => {
    const dePosse = TIPOS_CONTATO.filter((t) => EXIGE_POSSE[t])
    expect(dePosse).toEqual(['acompanhamento'])
  })

  test('interessado sugere assumir; os outros de reserva sugerem devolver', () => {
    expect(DESFECHO_SUGERIDO.interessado).toBe('assumir')
    expect(DESFECHO_SUGERIDO.nao_liguei).toBe('devolver')
    expect(DESFECHO_SUGERIDO.nao_atendeu).toBe('devolver')
    expect(DESFECHO_SUGERIDO.retornar_depois).toBe('devolver')
    expect(DESFECHO_SUGERIDO.sem_interesse).toBe('devolver')
    expect(DESFECHO_SUGERIDO.acompanhamento).toBe('nenhum')
  })
})

describe('ehTipo', () => {
  test('aceita tipo da lista', () => {
    expect(ehTipo('nao_liguei')).toBe(true)
  })

  test('recusa tipo fora da lista — e e a UNICA defesa que existe, porque o banco so exige nao-vazio', () => {
    expect(ehTipo('inventado')).toBe(false)
  })

  // Includes sobre tupla, não indexação de objeto: sem isso 'toString' passaria.
  test('nome de propriedade herdada nao e tipo', () => {
    expect(ehTipo('toString')).toBe(false)
  })
})
```

- [ ] **Step 2: Rode e veja falhar**

Run: `npm run test:unit -- tipos`
Esperado: FAIL, `Failed to resolve import './tipos'`.

- [ ] **Step 3: Implemente**

Crie `src/features/contato/tipos.ts`:

```ts
// Lista fechada aqui, e no banco só o piso de não-vazio. `usuario.papel` está
// no CHECK porque o BANCO o lê (eh_gestor); `contato.tipo` nenhuma função lê,
// então cada palavra nova aqui é uma linha de código, não uma migração.
// O preço: o banco aceita qualquer texto. A defesa é `ehTipo` e os testes.
export const TIPOS_CONTATO = [
  'nao_liguei',
  'nao_atendeu',
  'retornar_depois',
  'sem_interesse',
  'interessado',
  'acompanhamento',
] as const

export type TipoContato = (typeof TIPOS_CONTATO)[number]

export type Desfecho = 'nenhum' | 'assumir' | 'devolver'

export const ROTULO: Record<TipoContato, string> = {
  nao_liguei: 'Não liguei — descartei pelo cadastro',
  nao_atendeu: 'Liguei, não falei com ninguém',
  retornar_depois: 'Falei, pediu para ligar em outro momento',
  sem_interesse: 'Falei, não tem interesse',
  interessado: 'Falei, tem interesse',
  acompanhamento: 'Falei, e ficou combinado o próximo passo',
}

// Sugestão da TELA, não regra do banco. É a coerência desta escolha que
// mantém verdadeiro o desenho de o banco não interpretar `tipo`.
export const DESFECHO_SUGERIDO: Record<TipoContato, Desfecho> = {
  nao_liguei: 'devolver',
  nao_atendeu: 'devolver',
  retornar_depois: 'devolver',
  sem_interesse: 'devolver',
  interessado: 'assumir',
  acompanhamento: 'nenhum',
}

// Onde há posse o próximo passo é exigido; onde não há, não é pedido. Não é
// "esqueci de preencher", é "não existe o que preencher".
export const EXIGE_POSSE: Record<TipoContato, boolean> = {
  nao_liguei: false,
  nao_atendeu: false,
  retornar_depois: false,
  sem_interesse: false,
  interessado: false,
  acompanhamento: true,
}

const CONJUNTO: readonly string[] = TIPOS_CONTATO

export function ehTipo(v: string): v is TipoContato {
  return CONJUNTO.includes(v)
}
```

- [ ] **Step 4: Rode e veja passar**

Run: `npm run test:unit -- tipos`
Esperado: PASS.

- [ ] **Step 5: Commit**

```bash
git branch --show-current
git add src/features/contato/tipos.ts src/features/contato/tipos.test.ts
git commit -m "feat(contato): a lista fechada de tipos, com rótulo e desfecho sugerido"
```

---

### Task 3: `src/features/contato/regras.ts` — quando o próximo passo é exigido

**Files:**
- Create: `src/features/contato/regras.ts`
- Test: `src/features/contato/regras.test.ts`

**Interfaces:**
- Consumes: `TipoContato`, `Desfecho`, `ehTipo`, `EXIGE_POSSE` de `./tipos`.
- Produces:
  - `type Rascunho = { tipo: string; desfecho: string; nota: string; proximoPasso: string; proximoPassoData: string; posse: boolean }`
  - `type Falta = 'tipo_invalido' | 'desfecho_invalido' | 'proximo_passo_exigido' | 'par_incompleto'`
  - `type Validado = { tipo: TipoContato; desfecho: Desfecho; nota: string | null; proximoPasso: string | null; proximoPassoData: string | null }`
  - `validar(r: Rascunho): { ok: true; valor: Validado } | { ok: false; falta: Falta }`

- [ ] **Step 1: Escreva o teste**

Crie `src/features/contato/regras.test.ts`:

```ts
import { describe, expect, test } from 'vitest'
import { validar, type Rascunho } from './regras'

const BASE: Rascunho = {
  tipo: 'nao_atendeu',
  desfecho: 'devolver',
  nota: '',
  proximoPasso: '',
  proximoPassoData: '',
  posse: false,
}

describe('validar', () => {
  test('rascunho de reserva sem proximo passo e valido', () => {
    expect(validar(BASE)).toEqual({
      ok: true,
      valor: { tipo: 'nao_atendeu', desfecho: 'devolver', nota: null, proximoPasso: null, proximoPassoData: null },
    })
  })

  test('tipo fora da lista e recusado antes do banco', () => {
    expect(validar({ ...BASE, tipo: 'inventado' })).toEqual({ ok: false, falta: 'tipo_invalido' })
  })

  test('desfecho fora do vocabulario e recusado antes do banco', () => {
    expect(validar({ ...BASE, desfecho: 'talvez' })).toEqual({ ok: false, falta: 'desfecho_invalido' })
  })

  // A regra que existe por causa da posse. Sem ela, o vendedor assume uma
  // empresa e a agenda dele nasce vazia sem ninguém perceber.
  test('com posse, proximo passo e EXIGIDO', () => {
    const r = validar({ ...BASE, tipo: 'acompanhamento', desfecho: 'nenhum', posse: true })
    expect(r).toEqual({ ok: false, falta: 'proximo_passo_exigido' })
  })

  test('com posse e proximo passo completo, passa', () => {
    const r = validar({
      ...BASE,
      tipo: 'acompanhamento',
      desfecho: 'nenhum',
      posse: true,
      proximoPasso: 'Mandar orçamento',
      proximoPassoData: '2026-10-01',
    })
    expect(r).toEqual({
      ok: true,
      valor: {
        tipo: 'acompanhamento',
        desfecho: 'nenhum',
        nota: null,
        proximoPasso: 'Mandar orçamento',
        proximoPassoData: '2026-10-01',
      },
    })
  })

  const incompletos: [string, Partial<Rascunho>][] = [
    ['passo sem data', { proximoPasso: 'Ligar em março', proximoPassoData: '' }],
    ['data sem passo', { proximoPasso: '', proximoPassoData: '2026-10-01' }],
  ]
  for (const [nome, patch] of incompletos) {
    test(`${nome} e recusado antes do banco, e nao vira 23514 na tela`, () => {
      expect(validar({ ...BASE, ...patch })).toEqual({ ok: false, falta: 'par_incompleto' })
    })
  }

  // `retornar_depois` grava passo e data SEM posse de propósito: é o histórico
  // que a próxima pessoa lê e o dado que calibra o segundo prazo. A tela avisa
  // que a empresa volta em 30 dias; a regra não proíbe.
  test('retornar_depois grava passo e data mesmo sem posse', () => {
    const r = validar({
      ...BASE,
      tipo: 'retornar_depois',
      proximoPasso: 'Retomar contato',
      proximoPassoData: '2027-03-02',
    })
    expect(r).toEqual({
      ok: true,
      valor: {
        tipo: 'retornar_depois',
        desfecho: 'devolver',
        nota: null,
        proximoPasso: 'Retomar contato',
        proximoPassoData: '2027-03-02',
      },
    })
  })

  test('espaco em branco vira nulo, nao string vazia — o CHECK do banco recusa vazio', () => {
    const r = validar({ ...BASE, nota: '   ' })
    expect(r).toEqual({ ok: true, valor: expect.objectContaining({ nota: null }) })
  })
})
```

- [ ] **Step 2: Rode e veja falhar**

Run: `npm run test:unit -- regras.test`
Esperado: FAIL, `Failed to resolve import './regras'`.

Atenção: existe `src/features/usuarios/regras.test.ts`. Use o caminho completo se quiser isolar: `npm run test:unit -- src/features/contato/regras.test.ts`.

- [ ] **Step 3: Implemente**

Crie `src/features/contato/regras.ts`:

```ts
import { ehTipo, EXIGE_POSSE, type Desfecho, type TipoContato } from './tipos'

export type Rascunho = {
  tipo: string
  desfecho: string
  nota: string
  proximoPasso: string
  proximoPassoData: string
  posse: boolean
}

export type Falta = 'tipo_invalido' | 'desfecho_invalido' | 'proximo_passo_exigido' | 'par_incompleto'

export type Validado = {
  tipo: TipoContato
  desfecho: Desfecho
  nota: string | null
  proximoPasso: string | null
  proximoPassoData: string | null
}

const DESFECHOS: readonly string[] = ['nenhum', 'assumir', 'devolver']

function ouNulo(v: string): string | null {
  const t = v.trim()
  return t === '' ? null : t
}

export function validar(r: Rascunho): { ok: true; valor: Validado } | { ok: false; falta: Falta } {
  if (!ehTipo(r.tipo)) return { ok: false, falta: 'tipo_invalido' }
  if (!DESFECHOS.includes(r.desfecho)) return { ok: false, falta: 'desfecho_invalido' }

  const proximoPasso = ouNulo(r.proximoPasso)
  const proximoPassoData = ouNulo(r.proximoPassoData)

  // Espelha `contato_proximo_passo_coerente`. Recusar aqui é o que faz a tela
  // dizer "faltou a data" em vez de deixar o 23514 subir como erro de sistema.
  if ((proximoPasso === null) !== (proximoPassoData === null)) return { ok: false, falta: 'par_incompleto' }

  // Exige onde há posse, não pede onde não há.
  if (r.posse && EXIGE_POSSE[r.tipo] && proximoPasso === null) {
    return { ok: false, falta: 'proximo_passo_exigido' }
  }

  return {
    ok: true,
    valor: { tipo: r.tipo, desfecho: r.desfecho as Desfecho, nota: ouNulo(r.nota), proximoPasso, proximoPassoData },
  }
}
```

- [ ] **Step 4: Rode e veja passar**

Run: `npm run test:unit -- src/features/contato/regras.test.ts`
Esperado: PASS.

- [ ] **Step 5: Commit**

```bash
git branch --show-current
git add src/features/contato/regras.ts src/features/contato/regras.test.ts
git commit -m "feat(contato): a regra pura de quando o próximo passo é exigido"
```

---

### Task 4: Migração 0018 — `contato_registrar`

**Files:**
- Create: `db/migracoes/0018_contato_registrar.sql`
- Create: `tests/integracao/contato-registrar.test.ts`
- Modify: `src/server/db/migracoes/invariantes.ts:23-34`

**Interfaces:**
- Consumes: `contato` (0017), `empresa_assumir`, `empresa_devolver`, `pode_escrever()`, `usuario_atual()`.
- Produces: `contato_registrar(uuid, text, text, text, date, text) RETURNS text`, vocabulário `ok | nao_encontrada | reserva_expirada | ja_e_sua`, com `GRANT EXECUTE TO app_usuario`.

- [ ] **Step 1: Escreva o teste**

Crie `tests/integracao/contato-registrar.test.ts`:

```ts
import { afterAll, beforeAll, beforeEach, describe, expect, test } from 'vitest'
import { conectarVerificado } from '@/src/server/db/pool'
import { criarBancoDeTeste, criarUsuario, criarUsuarioComSenha, type BancoDeTeste } from './ajuda'

let banco: BancoDeTeste
let vendedorA: string
let vendedorB: string

beforeAll(async () => {
  banco = await criarBancoDeTeste()
  vendedorA = await criarUsuario(banco, 'vendedor', 'VendedorA')
  vendedorB = await criarUsuario(banco, 'vendedor', 'VendedorB')
})
afterAll(async () => {
  await banco.derrubar()
})

beforeEach(async () => {
  await banco.sql('DELETE FROM contato')
  await banco.sql('DELETE FROM empresa_fila')
  await banco.sql('DELETE FROM empresa')
})

async function criarEmpresa(sufixo = '0181'): Promise<string> {
  const [linha] = await banco.sql<{ id: string }>(
    `INSERT INTO empresa (cnpj, razao_social, telefone)
     VALUES ($1, $2, '11987654321') RETURNING id`,
    [`1122233300${sufixo}`, `Empresa ${sufixo}`],
  )
  return linha.id
}

type Args = {
  tipo?: string
  nota?: string | null
  proximoPasso?: string | null
  proximoPassoData?: string | null
  desfecho?: string
}

function registrar(usuarioId: string, empresaId: string, args: Args = {}): Promise<string> {
  return banco.comoUsuario(usuarioId, async (e) => {
    const r = await e<{ contato_registrar: string }>('SELECT contato_registrar($1, $2, $3, $4, $5, $6)', [
      empresaId,
      args.tipo ?? 'nao_atendeu',
      args.nota ?? null,
      args.proximoPasso ?? null,
      args.proximoPassoData ?? null,
      args.desfecho ?? 'nenhum',
    ])
    return r.linhas[0].contato_registrar
  })
}

function contarContatos(): Promise<number> {
  return banco.sql<{ n: string }>('SELECT count(*)::text AS n FROM contato').then(([l]) => Number(l.n))
}

describe('contato_registrar: quem pode', () => {
  test('empresa sem linha de fila e nao_encontrada', async () => {
    const id = await criarEmpresa()
    expect(await registrar(vendedorA, id)).toBe('nao_encontrada')
  })

  test('empresa que nao esta com voce e 42501', async () => {
    const id = await criarEmpresa()
    await banco.comoUsuario(vendedorB, (e) => e('SELECT empresa_id FROM fila_puxar()'))
    await expect(registrar(vendedorA, id)).rejects.toMatchObject({ code: '42501' })
  })

  test('reserva propria expirada e reserva_expirada, e NAO deixa contato', async () => {
    const id = await criarEmpresa()
    await banco.sql(
      `INSERT INTO empresa_fila (empresa_id, reservado_por, reservado_ate, primeira_reserva_em)
       VALUES ($1, $2, now() - interval '1 minute', now())`,
      [id, vendedorA],
    )
    expect(await registrar(vendedorA, id)).toBe('reserva_expirada')
    expect(await contarContatos()).toBe(0)
  })

  test('senha provisoria pendente e 42501', async () => {
    const id = await criarEmpresa()
    const pendente = await criarUsuarioComSenha(banco, 'vendedor', 'Pendente', 'segredo123', { pendente: true })
    await banco.sql(
      `INSERT INTO empresa_fila (empresa_id, reservado_por, reservado_ate, primeira_reserva_em)
       VALUES ($1, $2, now() + interval '30 minutes', now())`,
      [id, pendente.id],
    )
    await expect(registrar(pendente.id, id)).rejects.toMatchObject({ code: '42501' })
  })

  // Desfecho inventado é a aplicação passando lixo, não pergunta legítima
  // recusada: não é 42501 e não entra no vocabulário.
  test('desfecho fora do vocabulario LANCA, e nao vira 42501', async () => {
    const id = await criarEmpresa()
    await banco.comoUsuario(vendedorA, (e) => e('SELECT empresa_id FROM fila_puxar()'))
    const erro = await registrar(vendedorA, id, { desfecho: 'talvez' }).catch((e: { code?: string }) => e)
    expect((erro as { code?: string }).code).not.toBe('42501')
  })
})

describe('contato_registrar: os três desfechos', () => {
  test('nenhum grava o contato e nao mexe na fila', async () => {
    const id = await criarEmpresa()
    await banco.comoUsuario(vendedorA, (e) => e('SELECT empresa_id FROM fila_puxar()'))
    expect(await registrar(vendedorA, id, { desfecho: 'nenhum' })).toBe('ok')
    const [f] = await banco.sql<{ vendedor_id: string | null; reservado_por: string | null }>(
      'SELECT vendedor_id, reservado_por FROM empresa_fila WHERE empresa_id = $1',
      [id],
    )
    expect(f).toEqual({ vendedor_id: null, reservado_por: vendedorA })
    expect(await contarContatos()).toBe(1)
  })

  test('assumir grava posse E o contato', async () => {
    const id = await criarEmpresa()
    await banco.comoUsuario(vendedorA, (e) => e('SELECT empresa_id FROM fila_puxar()'))
    expect(await registrar(vendedorA, id, { tipo: 'interessado', desfecho: 'assumir' })).toBe('ok')
    const [f] = await banco.sql<{ vendedor_id: string | null }>(
      'SELECT vendedor_id FROM empresa_fila WHERE empresa_id = $1',
      [id],
    )
    expect(f.vendedor_id).toBe(vendedorA)
    expect(await contarContatos()).toBe(1)
  })

  test('devolver grava o piso E o contato', async () => {
    const id = await criarEmpresa()
    await banco.comoUsuario(vendedorA, (e) => e('SELECT empresa_id FROM fila_puxar()'))
    expect(await registrar(vendedorA, id, { desfecho: 'devolver' })).toBe('ok')
    const [f] = await banco.sql<{ vendedor_id: string | null; elegivel_em: Date | null }>(
      'SELECT vendedor_id, elegivel_em FROM empresa_fila WHERE empresa_id = $1',
      [id],
    )
    expect(f.vendedor_id).toBeNull()
    expect(f.elegivel_em).toBeInstanceOf(Date)
    expect(await contarContatos()).toBe(1)
  })

  test('ja_e_sua vem propagado de empresa_assumir, e nao deixa contato', async () => {
    const id = await criarEmpresa()
    await banco.comoUsuario(vendedorA, (e) => e('SELECT empresa_id FROM fila_puxar()'))
    await registrar(vendedorA, id, { tipo: 'interessado', desfecho: 'assumir' })
    await banco.sql('DELETE FROM contato')
    expect(await registrar(vendedorA, id, { tipo: 'interessado', desfecho: 'assumir' })).toBe('ja_e_sua')
    expect(await contarContatos()).toBe(0)
  })
})

// O passo 5 antes do passo 6 do contrato. Sem este teste, a ordem parece
// arbitrária e o próximo a mexer insere o contato primeiro — e aí um desfecho
// que falha deixa contato órfão, porque RETURN não desfaz INSERT em plpgsql.
describe('contato_registrar: desfecho que falha nao deixa contato', () => {
  test('reserva expirada com desfecho assumir devolve reserva_expirada e zero contatos', async () => {
    const id = await criarEmpresa()
    await banco.sql(
      `INSERT INTO empresa_fila (empresa_id, reservado_por, reservado_ate, primeira_reserva_em)
       VALUES ($1, $2, now() - interval '1 second', now())`,
      [id, vendedorA],
    )
    expect(await registrar(vendedorA, id, { tipo: 'interessado', desfecho: 'assumir' })).toBe('reserva_expirada')
    expect(await contarContatos()).toBe(0)
  })
})

describe('contato_registrar: o conteudo gravado', () => {
  test('grava tipo, nota, passo e data como vieram, e criado_por e quem chamou', async () => {
    const id = await criarEmpresa()
    await banco.comoUsuario(vendedorA, (e) => e('SELECT empresa_id FROM fila_puxar()'))
    await registrar(vendedorA, id, {
      tipo: 'retornar_depois',
      nota: 'pediu para ligar em marco',
      proximoPasso: 'Retomar contato',
      proximoPassoData: '2027-03-02',
      desfecho: 'devolver',
    })
    const [c] = await banco.sql<{
      tipo: string
      nota: string
      proximo_passo: string
      proximo_passo_data: Date
      criado_por: string
    }>('SELECT tipo, nota, proximo_passo, proximo_passo_data, criado_por FROM contato')
    expect(c.tipo).toBe('retornar_depois')
    expect(c.nota).toBe('pediu para ligar em marco')
    expect(c.proximo_passo).toBe('Retomar contato')
    expect(c.criado_por).toBe(vendedorA)
  })
})

// A trava. `contato_registrar` escreve em `empresa_fila` pelo desfecho, então
// vale para ela a mesma regra das três funções da 0016.
describe('contato_registrar x fila_puxar: a trava', () => {
  test('contato_registrar segurando a trava faz o fila_puxar concorrente PULAR a empresa', async () => {
    const unica = await criarEmpresa('0181')
    await banco.sql(
      `INSERT INTO empresa_fila (empresa_id, reservado_por, reservado_ate, primeira_reserva_em)
       VALUES ($1, $2, now() + interval '30 minutes', now())`,
      [unica, vendedorA],
    )
    const c1 = await conectarVerificado(banco.urlApp)
    const c2 = await conectarVerificado(banco.urlApp)
    try {
      await c1.query('BEGIN')
      await c1.query("SELECT set_config('role', 'app_usuario', true)")
      await c1.query("SELECT set_config('app.usuario_id', $1, true)", [vendedorA])
      await c1.query('SELECT contato_registrar($1, $2, NULL, NULL, NULL, $3)', [unica, 'interessado', 'assumir'])
      await c2.query('BEGIN')
      await c2.query("SELECT set_config('role', 'app_usuario', true)")
      await c2.query("SELECT set_config('app.usuario_id', $1, true)", [vendedorB])
      const puxada = await c2.query('SELECT empresa_id FROM fila_puxar()')
      await c1.query('COMMIT')
      await c2.query('COMMIT')
      expect(puxada.rows).toEqual([])
      const [estado] = await banco.sql<{ vendedor_id: string }>(
        'SELECT vendedor_id FROM empresa_fila WHERE empresa_id = $1',
        [unica],
      )
      expect(estado.vendedor_id).toBe(vendedorA)
    } finally {
      c1.release()
      c2.release()
    }
  })
})
```

- [ ] **Step 2: Rode e veja falhar**

Run: `npm run test:integracao -- contato-registrar`
Esperado: FAIL, `function contato_registrar(...) does not exist`.

- [ ] **Step 3: Escreva a migração**

Crie `db/migracoes/0018_contato_registrar.sql`:

```sql
-- ver docs/db/0018.md
BEGIN;
CREATE FUNCTION contato_registrar(
  p_empresa_id         uuid,
  p_tipo               text,
  p_nota               text,
  p_proximo_passo      text,
  p_proximo_passo_data date,
  p_desfecho           text
) RETURNS text
LANGUAGE plpgsql VOLATILE SECURITY DEFINER SET search_path = ''
AS $$
DECLARE
  v_eu       uuid := public.usuario_atual();
  v_dono     uuid;
  v_resv     uuid;
  v_ate      timestamptz;
  v_desfecho text;
BEGIN
  -- Desfecho inventado é a aplicação passando lixo, não pergunta legítima
  -- recusada: exceção crua, não 42501, e fora do vocabulário de propósito.
  IF p_desfecho IS NULL OR p_desfecho NOT IN ('nenhum', 'assumir', 'devolver') THEN
    RAISE EXCEPTION 'desfecho invalido: %', p_desfecho;
  END IF;

  IF NOT public.pode_escrever() THEN
    RAISE EXCEPTION 'troque a senha provisoria antes de registrar contato' USING ERRCODE = '42501';
  END IF;

  -- A ordem é a mesma das três funções da 0016: travar `empresa` antes de
  -- tudo, inclusive antes de inserir o contato. Invertê-la (inserir e travar
  -- depois) cria um caminho de deadlock que NÃO aparece em teste de uma
  -- conexão só — com uma conexão nunca há duas ordens concorrentes.
  PERFORM 1 FROM public.empresa e WHERE e.id = p_empresa_id FOR UPDATE;

  SELECT f.vendedor_id, f.reservado_por, f.reservado_ate
    INTO v_dono, v_resv, v_ate
    FROM public.empresa_fila f
   WHERE f.empresa_id = p_empresa_id;
  IF NOT FOUND THEN
    RETURN 'nao_encontrada';
  END IF;

  -- IS DISTINCT FROM, não `=`: com `v_resv` nulo, `v_resv = v_eu` devolve NULL
  -- e a checagem inteira seria pulada. É a mesma lição do COALESCE de
  -- empresa_devolver, resolvida pelo operador em vez do default explícito.
  IF v_dono IS DISTINCT FROM v_eu AND v_resv IS DISTINCT FROM v_eu THEN
    RAISE EXCEPTION 'esta empresa nao esta com voce' USING ERRCODE = '42501';
  END IF;

  -- Só chega aqui quem tem reserva própria; o CHECK de coerência garante que
  -- `reservado_ate` não é nulo quando `reservado_por` não é.
  IF v_dono IS DISTINCT FROM v_eu AND v_ate <= now() THEN
    RETURN 'reserva_expirada';
  END IF;

  -- O desfecho vem ANTES do INSERT, e é contrato. Em plpgsql um RETURN depois
  -- de um INSERT não desfaz o INSERT: aplicando o desfecho primeiro, desfecho
  -- que falha deixa zero contato, sem precisar de subtransação.
  IF p_desfecho = 'assumir' THEN
    v_desfecho := public.empresa_assumir(p_empresa_id);
  ELSIF p_desfecho = 'devolver' THEN
    v_desfecho := public.empresa_devolver(p_empresa_id);
  END IF;
  IF v_desfecho IS NOT NULL AND v_desfecho <> 'ok' THEN
    RETURN v_desfecho;
  END IF;

  INSERT INTO public.contato (empresa_id, tipo, nota, proximo_passo, proximo_passo_data)
  VALUES (p_empresa_id, p_tipo, p_nota, p_proximo_passo, p_proximo_passo_data);
  RETURN 'ok';
END;
$$;
REVOKE EXECUTE ON FUNCTION contato_registrar(uuid, text, text, text, date, text) FROM PUBLIC;
GRANT EXECUTE ON FUNCTION contato_registrar(uuid, text, text, text, date, text) TO app_usuario;
COMMIT;
```

- [ ] **Step 4: Registre a função na invariante**

Modifique `src/server/db/migracoes/invariantes.ts:23-34`, acrescentando a linha no fim da lista:

```ts
export const FUNCOES_CONCEDIDAS_A_APP_USUARIO = [
  'public.usuario_atual',
  'public.pode_ler',
  'public.eh_gestor',
  'public.pode_escrever',
  'public.senha_provisoria_de',
  'public.credencial_definir',
  'public.usuario_situacao_definir',
  'public.fila_puxar',
  'public.empresa_assumir',
  'public.empresa_devolver',
  'public.contato_registrar',
] as const
```

- [ ] **Step 5: Rode e veja passar**

```
npm run db:checar
npm run test:integracao -- contato-registrar
npm run test:unit -- invariantes
```

Esperado: PASS em todos. Se a invariante reclamar `função definidora concedida a app_usuario e não registrada`, o passo 4 não foi feito.

- [ ] **Step 6: Commit**

```bash
git branch --show-current
git add db/migracoes/0018_contato_registrar.sql tests/integracao/contato-registrar.test.ts src/server/db/migracoes/invariantes.ts
git commit -m "feat(db): 0018 — contato_registrar grava o contato e aplica o desfecho numa transação"
```

---

### Task 5: Migração 0019 — `empresa_assumir` e `empresa_devolver` viram internas

**Files:**
- Create: `db/migracoes/0019_fila_interna.sql`
- Modify: `src/server/db/migracoes/invariantes.ts:23-34`
- Modify: `tests/integracao/fila-posse.test.ts`
- Modify: `src/features/fila/repositorio.ts`
- Modify: `src/features/fila/repositorio.test.ts`

**Interfaces:**
- Consumes: `contato_registrar` (0018).
- Produces: `empresa_assumir` e `empresa_devolver` sem `GRANT` para `app_usuario`; `src/features/fila/repositorio.ts` exportando só `puxarProxima`, `traduzirResultado` e `ResultadoDesconhecido`.

**Atenção — esta tarefa QUEBRA testes existentes de propósito.** `tests/integracao/fila-posse.test.ts` chama `empresa_assumir` e `empresa_devolver` como `app_usuario`. Depois da revogação, todas essas chamadas viram `42501`. A reescrita faz parte da tarefa, e o caminho é passar por `contato_registrar` — o contrato de ordem de checagens continua exercitado, agora pelo chamador de verdade.

- [ ] **Step 1: Escreva o teste da revogação**

Acrescente ao fim de `tests/integracao/contato-politicas.test.ts`:

```ts
// A revogação. É este teste que impede o GRANT de voltar por descuido numa
// migração futura: a ausência do privilégio não é observável no código.
describe('empresa_assumir e empresa_devolver sao internas', () => {
  const internas: [string, string][] = [
    ['empresa_assumir', 'SELECT empresa_assumir($1)'],
    ['empresa_devolver', 'SELECT empresa_devolver($1)'],
  ]
  for (const [nome, sql] of internas) {
    test(`${nome} chamada direto por app_usuario e 42501`, async () => {
      await expect(banco.comoUsuario(vendedorA, (e) => e(sql, [empresa]))).rejects.toMatchObject({ code: '42501' })
    })
  }
})
```

- [ ] **Step 2: Rode e veja falhar**

Run: `npm run test:integracao -- contato-politicas`
Esperado: FAIL — as duas chamadas ainda funcionam, então `rejects` não acontece.

- [ ] **Step 3: Escreva a migração**

Crie `db/migracoes/0019_fila_interna.sql`:

```sql
-- ver docs/db/0019.md
BEGIN;
-- As duas ficaram sem chamador quando `contato_registrar` passou a ser o único
-- caminho de desfecho da tela. Superfície concedida sem uso não é neutra
-- (R-014): ninguém a testa, ninguém lembra por que existe, e ela continua
-- chamável. Continuam existindo e sendo chamadas por dentro da 0018.
REVOKE EXECUTE ON FUNCTION empresa_assumir(uuid) FROM app_usuario;
REVOKE EXECUTE ON FUNCTION empresa_devolver(uuid) FROM app_usuario;
COMMIT;
```

- [ ] **Step 4: Tire as duas da invariante**

Modifique `src/server/db/migracoes/invariantes.ts`, removendo as duas linhas:

```ts
export const FUNCOES_CONCEDIDAS_A_APP_USUARIO = [
  'public.usuario_atual',
  'public.pode_ler',
  'public.eh_gestor',
  'public.pode_escrever',
  'public.senha_provisoria_de',
  'public.credencial_definir',
  'public.usuario_situacao_definir',
  'public.fila_puxar',
  'public.contato_registrar',
] as const
```

A invariante confere os dois sentidos: deixar `public.empresa_assumir` na lista depois do `REVOKE` derruba o `db:aplicar` com `função registrada e ausente ou sem GRANT`.

- [ ] **Step 5: Reescreva os helpers de `fila-posse.test.ts`**

Em `tests/integracao/fila-posse.test.ts`, troque as funções `assumir` e `devolver` e o comentário do topo do `describe` da trava:

```ts
// `empresa_assumir` e `empresa_devolver` são internas desde a 0019: o caminho
// da aplicação é `contato_registrar` com desfecho. O contrato de ordem das
// checagens continua o mesmo e continua exercitado — só que pelo chamador de
// verdade, que é o que a tela usa.
function assumir(usuarioId: string, empresaId: string): Promise<string> {
  return banco.comoUsuario(usuarioId, async (e) => {
    const r = await e<{ contato_registrar: string }>(
      'SELECT contato_registrar($1, $2, NULL, NULL, NULL, $3)',
      [empresaId, 'interessado', 'assumir'],
    )
    return r.linhas[0].contato_registrar
  })
}

function devolver(usuarioId: string, empresaId: string): Promise<string> {
  return banco.comoUsuario(usuarioId, async (e) => {
    const r = await e<{ contato_registrar: string }>(
      'SELECT contato_registrar($1, $2, NULL, NULL, NULL, $3)',
      [empresaId, 'nao_atendeu', 'devolver'],
    )
    return r.linhas[0].contato_registrar
  })
}
```

Nos dois testes de concorrência do `describe('empresa_assumir x fila_puxar: a trava')`, troque as chamadas cruas:

```ts
// era: await c1.query('SELECT empresa_assumir($1)', [unica])
await c1.query('SELECT contato_registrar($1, $2, NULL, NULL, NULL, $3)', [unica, 'interessado', 'assumir'])
```

```ts
// era: const espera = c2.query('SELECT empresa_assumir($1)', [unica])
const espera = c2.query('SELECT contato_registrar($1, $2, NULL, NULL, NULL, $3)', [unica, 'interessado', 'assumir'])
```

Acrescente `await banco.sql('DELETE FROM contato')` como primeira linha do `beforeEach` do arquivo — `contato` tem FK para `empresa`, e apagar empresa antes derruba com `23503`.

- [ ] **Step 6: Tire `assumir` e `devolver` do repositório da fila**

Em `src/features/fila/repositorio.ts`, remova as funções `assumir` e `devolver` (as duas últimas do arquivo). `puxarProxima`, `tentar`, `traduzirErro`, `traduzirResultado`, `VOCABULARIO`, `ResultadoDesconhecido`, `Motivo` e `Falha` ficam — `traduzirResultado` continua tendo consumidor pelo `puxarProxima`? Não: `puxarProxima` não usa vocabulário de texto. Mantenha `traduzirResultado` e o `VOCABULARIO` **apenas se** `src/features/fila/repositorio.test.ts` continuar cobrindo, e mova a responsabilidade para `src/features/contato/repositorio.ts` na Task 6. Se depois da Task 6 nada em `features/fila` usar `traduzirResultado`, apague-o do arquivo e o bloco correspondente do teste: é a mesma R-014 que motivou esta tarefa.

Nesta tarefa, faça o mínimo: remova `assumir` e `devolver`, e deixe um comentário no lugar:

```ts
// `assumir` e `devolver` saíram na 0019: as funções do banco viraram internas
// e o caminho da tela é `contato_registrar`, em src/features/contato.
```

- [ ] **Step 7: Rode tudo e veja passar**

```
npm run db:checar
npm run typecheck
npm run test:integracao
npm run test:unit
```

Esperado: PASS. `app/fila/acoes.ts` vai quebrar o `typecheck` porque importa `assumir` e `devolver` — **isso é esperado e é consertado na Task 11.** Para manter esta tarefa verde, aplique já a poda mínima em `app/fila/acoes.ts`: remova o import e os dois ramos, deixando só `puxar`, e ajuste `ACOES` para `['puxar']`. Os botões do cartão são refeitos na Task 11.

- [ ] **Step 8: Commit**

```bash
git branch --show-current
git add db/migracoes/0019_fila_interna.sql src/server/db/migracoes/invariantes.ts tests/integracao/fila-posse.test.ts tests/integracao/contato-politicas.test.ts src/features/fila/repositorio.ts app/fila/acoes.ts
git commit -m "feat(db): 0019 — empresa_assumir e empresa_devolver viram internas"
```

---

### Task 6: `src/features/contato/repositorio.ts` e `mensagens.ts`

**Files:**
- Create: `src/features/contato/repositorio.ts`
- Create: `src/features/contato/mensagens.ts`
- Test: `src/features/contato/repositorio.test.ts`
- Test: `src/features/contato/mensagens.test.ts`

**Interfaces:**
- Consumes: `comoUsuario`, `type Executar` de `@/src/server/db/como-usuario`; `Validado` de `./regras`.
- Produces:
  - `type Motivo = 'sem_permissao' | 'nao_encontrada' | 'reserva_expirada' | 'ja_e_sua'`
  - `type Falha = { ok: false; motivo: Motivo }`
  - `class ResultadoDesconhecido extends Error`
  - `traduzirResultado(funcao: string, valor: string): { ok: true } | Falha`
  - `registrarContato(usuarioId: string, empresaId: string, v: Validado): Promise<{ ok: true } | Falha>`
  - `textoDoMotivo(m: Motivo): string` em `mensagens.ts`

- [ ] **Step 1: Escreva os testes**

Crie `src/features/contato/repositorio.test.ts` (molde: `src/features/fila/repositorio.test.ts`):

```ts
import { describe, expect, test } from 'vitest'
import { ResultadoDesconhecido, traduzirResultado } from './repositorio'

describe('traduzirResultado', () => {
  test('ok vira sucesso', () => {
    expect(traduzirResultado('contato_registrar', 'ok')).toEqual({ ok: true })
  })

  const recusas: [string, string][] = [
    ['nao_encontrada', 'nao_encontrada'],
    ['reserva_expirada', 'reserva_expirada'],
    ['ja_e_sua', 'ja_e_sua'],
  ]
  for (const [valor, motivo] of recusas) {
    test(`${valor} vira falha com motivo proprio`, () => {
      expect(traduzirResultado('contato_registrar', valor)).toEqual({ ok: false, motivo })
    })
  }

  test('valor fora do vocabulario LANCA, nao vira falha', () => {
    expect(() => traduzirResultado('contato_registrar', 'talvez')).toThrow(ResultadoDesconhecido)
  })

  test('nome de propriedade herdada nao vira resultado', () => {
    expect(() => traduzirResultado('contato_registrar', 'toString')).toThrow(ResultadoDesconhecido)
  })
})
```

Crie `src/features/contato/mensagens.test.ts`:

```ts
import { describe, expect, test } from 'vitest'
import type { Falta } from './regras'
import type { Motivo } from './repositorio'
import { textoDaFalta, textoDoMotivo } from './mensagens'

describe('textoDoMotivo', () => {
  const motivos: Motivo[] = ['sem_permissao', 'nao_encontrada', 'reserva_expirada', 'ja_e_sua']
  for (const m of motivos) {
    test(`${m} tem texto proprio e nao vazio`, () => {
      expect(textoDoMotivo(m).length).toBeGreaterThan(0)
    })
  }

  test('reserva_expirada explica que o tempo acabou, e nao fala em permissao', () => {
    expect(textoDoMotivo('reserva_expirada')).not.toContain('permissão')
  })
})

describe('textoDaFalta', () => {
  const faltas: Falta[] = ['tipo_invalido', 'desfecho_invalido', 'proximo_passo_exigido', 'par_incompleto']
  for (const f of faltas) {
    test(`${f} tem texto proprio`, () => {
      expect(textoDaFalta(f).length).toBeGreaterThan(0)
    })
  }

  test('proximo_passo_exigido diz o que fazer, nao so que faltou', () => {
    expect(textoDaFalta('proximo_passo_exigido')).toContain('próximo passo')
  })
})
```

`textoDaFalta` mora em `mensagens.ts`, e não na action, por uma razão mecânica: arquivo `'use server'` só exporta função, e função exportada de lá é sempre `async` — o teste teria que esperar uma Promise para ler `.length`. Texto de tela não tem por que ser assíncrono.

- [ ] **Step 2: Rode e veja falhar**

Run: `npm run test:unit -- src/features/contato`
Esperado: FAIL, imports não resolvidos.

- [ ] **Step 3: Implemente**

Crie `src/features/contato/repositorio.ts`:

```ts
import { comoUsuario, type Executar } from '../../server/db/como-usuario'
import type { Validado } from './regras'

export type Motivo = 'sem_permissao' | 'nao_encontrada' | 'reserva_expirada' | 'ja_e_sua'
export type Falha = { ok: false; motivo: Motivo }

export class ResultadoDesconhecido extends Error {
  constructor(funcao: string, valor: string) {
    super(`${funcao} devolveu valor fora do vocabulário: ${JSON.stringify(valor)}`)
    this.name = 'ResultadoDesconhecido'
  }
}

// Object.hasOwn, não indexação direta, para 'toString' não virar resultado.
// Valor fora daqui é defeito nosso, não estado de negócio: lança.
const VOCABULARIO: Record<string, { ok: true } | Falha> = {
  ok: { ok: true },
  nao_encontrada: { ok: false, motivo: 'nao_encontrada' },
  reserva_expirada: { ok: false, motivo: 'reserva_expirada' },
  ja_e_sua: { ok: false, motivo: 'ja_e_sua' },
}

export function traduzirResultado(funcao: string, valor: string): { ok: true } | Falha {
  if (!Object.hasOwn(VOCABULARIO, valor)) throw new ResultadoDesconhecido(funcao, valor)
  return VOCABULARIO[valor]
}

function traduzirErro(erro: unknown): Falha | null {
  if ((erro as { code?: string })?.code === '42501') return { ok: false, motivo: 'sem_permissao' }
  return null
}

async function tentar<T>(usuarioId: string, trabalho: (executar: Executar) => Promise<T>): Promise<T | Falha> {
  try {
    return await comoUsuario(usuarioId, trabalho)
  } catch (erro) {
    const falha = traduzirErro(erro)
    if (falha) return falha
    throw erro
  }
}

export function registrarContato(
  usuarioId: string,
  empresaId: string,
  v: Validado,
): Promise<{ ok: true } | Falha> {
  return tentar(usuarioId, async (executar) => {
    const r = await executar<{ contato_registrar: string }>(
      'SELECT contato_registrar($1, $2, $3, $4, $5, $6)',
      [empresaId, v.tipo, v.nota, v.proximoPasso, v.proximoPassoData, v.desfecho],
    )
    return traduzirResultado('contato_registrar', r.linhas[0].contato_registrar)
  })
}
```

Crie `src/features/contato/mensagens.ts`:

```ts
import type { Falta } from './regras'
import type { Motivo } from './repositorio'

// Record, não if encadeado: motivo novo sem texto quebra o build.
const TEXTO_DO_MOTIVO: Record<Motivo, string> = {
  sem_permissao: 'Você não tem permissão para isso agora. Se acabou de trocar a senha, entre de novo.',
  nao_encontrada: 'Esta empresa não está mais na fila. Puxe a próxima.',
  reserva_expirada: 'O tempo da reserva acabou e a empresa voltou para a fila. O contato não foi registrado.',
  ja_e_sua: 'Esta empresa já está na sua carteira.',
}

export function textoDoMotivo(m: Motivo): string {
  return TEXTO_DO_MOTIVO[m]
}

const TEXTO_DA_FALTA: Record<Falta, string> = {
  tipo_invalido: 'Escolha o que aconteceu na ligação.',
  desfecho_invalido: 'Escolha o que fazer com a empresa.',
  proximo_passo_exigido: 'Empresa da sua carteira precisa de um próximo passo com data.',
  par_incompleto: 'Próximo passo e data andam juntos: preencha os dois ou nenhum.',
}

export function textoDaFalta(f: Falta): string {
  return TEXTO_DA_FALTA[f]
}
```

- [ ] **Step 4: Rode e veja passar**

Run: `npm run test:unit -- src/features/contato`
Esperado: PASS.

- [ ] **Step 5: Commit**

```bash
git branch --show-current
git add src/features/contato/repositorio.ts src/features/contato/repositorio.test.ts src/features/contato/mensagens.ts src/features/contato/mensagens.test.ts
git commit -m "feat(contato): repositório e mensagens, com o vocabulário traduzido num lugar só"
```

---

### Task 7: `src/features/contato/historico.ts` — a linha do tempo

**Files:**
- Create: `src/features/contato/historico.ts`
- Test: `tests/integracao/contato-historico.test.ts`

**Interfaces:**
- Consumes: `comoUsuario`.
- Produces:
  - `type Contato = { id: string; tipo: string; nota: string | null; proximoPasso: string | null; proximoPassoData: string | null; criadoEm: Date; autor: string | null }`
  - `lerHistorico(usuarioId: string, empresaId: string): Promise<{ ok: true; contatos: Contato[] } | Falha>`

- [ ] **Step 1: Escreva o teste de integração**

Crie `tests/integracao/contato-historico.test.ts`:

```ts
import { afterAll, beforeAll, beforeEach, describe, expect, test } from 'vitest'
import { lerHistorico } from '@/src/features/contato/historico'
import { criarBancoDeTeste, criarUsuario, type BancoDeTeste } from './ajuda'

let banco: BancoDeTeste
let vendedorA: string
let vendedorB: string
let empresa: string

beforeAll(async () => {
  banco = await criarBancoDeTeste()
  vendedorA = await criarUsuario(banco, 'vendedor', 'VendedorA')
  vendedorB = await criarUsuario(banco, 'vendedor', 'VendedorB')
  const [linha] = await banco.sql<{ id: string }>(
    `INSERT INTO empresa (cnpj, razao_social, telefone)
     VALUES ('11222333000181', 'Aurora Comercio LTDA', '11987654321') RETURNING id`,
  )
  empresa = linha.id
})
afterAll(async () => {
  await banco.derrubar()
})

beforeEach(async () => {
  await banco.sql('DELETE FROM contato')
  await banco.sql('DELETE FROM empresa_fila')
})

async function darPosseA(usuarioId: string): Promise<void> {
  await banco.sql(
    `INSERT INTO empresa_fila (empresa_id, vendedor_id, primeira_reserva_em)
     VALUES ($1, $2, now())
     ON CONFLICT (empresa_id) DO UPDATE
        SET vendedor_id = excluded.vendedor_id, reservado_por = NULL, reservado_ate = NULL`,
    [empresa, usuarioId],
  )
}

describe('lerHistorico', () => {
  test('empresa sem contato devolve lista vazia, e nao falha', async () => {
    await darPosseA(vendedorA)
    const r = await lerHistorico(vendedorA, empresa)
    expect(r).toEqual({ ok: true, contatos: [] })
  })

  test('devolve do mais recente para tras, com o nome do autor', async () => {
    await darPosseA(vendedorA)
    await banco.sql(
      `INSERT INTO contato (empresa_id, tipo, nota, criado_por, criado_em)
       VALUES ($1, 'nao_atendeu', 'primeiro', $2, now() - interval '2 days'),
              ($1, 'interessado', 'segundo', $2, now() - interval '1 day')`,
      [empresa, vendedorA],
    )
    const r = await lerHistorico(vendedorA, empresa)
    if (!r.ok) throw new Error('esperava sucesso')
    expect(r.contatos.map((c) => c.nota)).toEqual(['segundo', 'primeiro'])
    expect(r.contatos[0].autor).toBe('VendedorA')
  })

  test('quem nao esta com a empresa recebe lista vazia pela RLS, nao erro', async () => {
    await darPosseA(vendedorA)
    await banco.sql('INSERT INTO contato (empresa_id, tipo, criado_por) VALUES ($1, $2, $3)', [
      empresa,
      'nao_atendeu',
      vendedorA,
    ])
    const r = await lerHistorico(vendedorB, empresa)
    expect(r).toEqual({ ok: true, contatos: [] })
  })
})
```

- [ ] **Step 2: Rode e veja falhar**

Run: `npm run test:integracao -- contato-historico`
Esperado: FAIL, import não resolvido.

- [ ] **Step 3: Implemente**

Crie `src/features/contato/historico.ts`:

```ts
import { comoUsuario } from '../../server/db/como-usuario'
import type { Falha } from './repositorio'

export type Contato = {
  id: string
  tipo: string
  nota: string | null
  proximoPasso: string | null
  proximoPassoData: string | null
  criadoEm: Date
  autor: string | null
}

export type ResultadoHistorico = { ok: true; contatos: Contato[] } | Falha

type LinhaCrua = {
  id: string
  tipo: string
  nota: string | null
  proximo_passo: string | null
  proximo_passo_data: string | null
  criado_em: Date
  autor: string | null
}

// A RLS já filtra: quem não lê a empresa não lê o contato, e o resultado é
// lista vazia, não erro. `criado_em DESC, id DESC` é a ordem do índice, com
// desempate estável pelo mesmo motivo do `e.id ASC` da fila.
const SQL = `SELECT c.id, c.tipo, c.nota, c.proximo_passo,
                    to_char(c.proximo_passo_data, 'YYYY-MM-DD') AS proximo_passo_data,
                    c.criado_em, u.nome AS autor
               FROM contato c
               LEFT JOIN usuario u ON u.id = c.criado_por
              WHERE c.empresa_id = $1
              ORDER BY c.criado_em DESC, c.id DESC`

export async function lerHistorico(usuarioId: string, empresaId: string): Promise<ResultadoHistorico> {
  try {
    return await comoUsuario(usuarioId, async (executar) => {
      const r = await executar<LinhaCrua>(SQL, [empresaId])
      return {
        ok: true as const,
        contatos: r.linhas.map((l) => ({
          id: l.id,
          tipo: l.tipo,
          nota: l.nota,
          proximoPasso: l.proximo_passo,
          proximoPassoData: l.proximo_passo_data,
          criadoEm: l.criado_em,
          autor: l.autor,
        })),
      }
    })
  } catch (erro) {
    if ((erro as { code?: string })?.code === '42501') return { ok: false, motivo: 'sem_permissao' }
    throw erro
  }
}
```

`to_char` devolve a data como texto `YYYY-MM-DD`: `date` do Postgres chega no `pg` como `string` em alguns caminhos e `Date` em outros, e converter no SQL tira a ambiguidade do TypeScript. Nenhuma comparação de data acontece aqui.

- [ ] **Step 4: Rode e veja passar**

Run: `npm run test:integracao -- contato-historico`
Esperado: PASS.

- [ ] **Step 5: Commit**

```bash
git branch --show-current
git add src/features/contato/historico.ts tests/integracao/contato-historico.test.ts
git commit -m "feat(contato): a linha do tempo de uma empresa, filtrada pela RLS"
```

---

### Task 8: A carteira ganha o próximo passo derivado

**Files:**
- Modify: `src/features/fila/consulta.ts`
- Test: `tests/integracao/fila-consulta.test.ts` (existente — acrescentar)
- Modify: `app/fila/cartao.test.tsx`, `app/carteira/lista.test.tsx` (fixtures)

**Interfaces:**
- Produces: `EmpresaComigo` ganha `proximoPasso: string | null`, `proximoPassoData: string | null`, `vencido: boolean`. `carteira` passa a vir ordenada por `proximo_passo_data ASC NULLS LAST`.

- [ ] **Step 1: Escreva o teste**

Acrescente a `tests/integracao/fila-consulta.test.ts` (abra o arquivo antes e reuse os helpers que já existem lá):

```ts
describe('carteira: o proximo passo derivado do contato mais recente', () => {
  test('empresa sem contato vem com proximo passo nulo e vencido falso', async () => {
    // (prepare posse para o vendedor, no molde dos testes já existentes no arquivo)
    const r = await lerMinhasEmpresas(vendedorA)
    if (!r.ok) throw new Error('esperava sucesso')
    expect(r.carteira[0]).toMatchObject({ proximoPasso: null, proximoPassoData: null, vencido: false })
  })

  test('o contato MAIS RECENTE manda', async () => {
    await banco.sql(
      `INSERT INTO contato (empresa_id, tipo, proximo_passo, proximo_passo_data, criado_em)
       VALUES ($1, 'acompanhamento', 'antigo', '2026-01-01', now() - interval '2 days'),
              ($1, 'acompanhamento', 'recente', '2027-01-01', now() - interval '1 day')`,
      [empresaComPosse],
    )
    const r = await lerMinhasEmpresas(vendedorA)
    if (!r.ok) throw new Error('esperava sucesso')
    expect(r.carteira[0].proximoPasso).toBe('recente')
  })

  // A decisão que evita o compromisso ressuscitado: registrar contato SEM
  // próximo passo significa "não há próximo passo", e não "vale o anterior".
  test('contato mais recente SEM passo apaga o passo anterior', async () => {
    await banco.sql(
      `INSERT INTO contato (empresa_id, tipo, proximo_passo, proximo_passo_data, criado_em)
       VALUES ($1, 'acompanhamento', 'combinado', '2027-01-01', now() - interval '2 days')`,
      [empresaComPosse],
    )
    await banco.sql(
      `INSERT INTO contato (empresa_id, tipo, criado_em)
       VALUES ($1, 'acompanhamento', now() - interval '1 day')`,
      [empresaComPosse],
    )
    const r = await lerMinhasEmpresas(vendedorA)
    if (!r.ok) throw new Error('esperava sucesso')
    expect(r.carteira[0].proximoPasso).toBeNull()
  })

  test('data de ontem vem como vencido; a de amanha nao', async () => {
    await banco.sql(
      `INSERT INTO contato (empresa_id, tipo, proximo_passo, proximo_passo_data)
       VALUES ($1, 'acompanhamento', 'atrasado', (now() AT TIME ZONE 'America/Sao_Paulo')::date - 1)`,
      [empresaComPosse],
    )
    const r = await lerMinhasEmpresas(vendedorA)
    if (!r.ok) throw new Error('esperava sucesso')
    expect(r.carteira[0].vencido).toBe(true)
  })

  test('a carteira vem ordenada por data, com os sem passo por ultimo', async () => {
    // três empresas: uma vencida, uma futura, uma sem passo
    const r = await lerMinhasEmpresas(vendedorA)
    if (!r.ok) throw new Error('esperava sucesso')
    expect(r.carteira.map((c) => c.proximoPasso)).toEqual(['vencida', 'futura', null])
  })
})
```

- [ ] **Step 2: Rode e veja falhar**

Run: `npm run test:integracao -- fila-consulta`
Esperado: FAIL, `proximoPasso` não existe em `EmpresaComigo`.

- [ ] **Step 3: Implemente**

Em `src/features/fila/consulta.ts`, acrescente ao tipo:

```ts
  // Derivado do contato mais recente, sem cache: `contato` é a verdade e
  // duas fontes para o mesmo dado sempre divergem. `vencido` é calculado no
  // banco, em data civil de São Paulo — nenhuma comparação de data em TS.
  proximoPasso: string | null
  proximoPassoData: string | null
  vencido: boolean
```

Acrescente a `LinhaCrua`:

```ts
  proximo_passo: string | null
  proximo_passo_data: string | null
  vencido: boolean
```

Troque a constante `SQL` por:

```ts
// A reserva vigente e a carteira numa consulta só. O LEFT JOIN LATERAL traz o
// contato mais recente por empresa; `c.id DESC` é desempate estável, porque
// `criado_em` vem de now() (início da transação) e ordem sem terceira chave
// vira sorteio quando há empate.
//
// A ordem é `proximo_passo_data ASC NULLS LAST`: vencido cai no topo sozinho,
// porque data menor vem antes. NULLS LAST é decisão — empresa sem próximo
// passo precisa de atenção, mas menos que um combinado vencido há três dias.
const SQL = `SELECT e.id, e.cnpj, e.razao_social, e.nome_fantasia, e.contato_nome, e.telefone, e.email, e.cep,
                    f.reservado_ate, f.vendedor_id IS NOT NULL AS posse,
                    ultimo.proximo_passo,
                    to_char(ultimo.proximo_passo_data, 'YYYY-MM-DD') AS proximo_passo_data,
                    coalesce(ultimo.proximo_passo_data < (now() AT TIME ZONE 'America/Sao_Paulo')::date, false) AS vencido
               FROM empresa_fila f
               JOIN empresa e ON e.id = f.empresa_id
               LEFT JOIN LATERAL (
                 SELECT c.proximo_passo, c.proximo_passo_data
                   FROM contato c
                  WHERE c.empresa_id = e.id
                  ORDER BY c.criado_em DESC, c.id DESC
                  LIMIT 1
               ) ultimo ON true
              WHERE f.vendedor_id = $1
                 OR (f.reservado_por = $1 AND f.reservado_ate > now())
              ORDER BY ultimo.proximo_passo_data ASC NULLS LAST, e.razao_social, e.id`
```

E no `map`, acrescente os três campos:

```ts
        proximoPasso: l.proximo_passo,
        proximoPassoData: l.proximo_passo_data,
        vencido: l.vencido,
```

- [ ] **Step 4: Conserte as fixtures dos testes de render**

`app/fila/cartao.test.tsx` e `app/carteira/lista.test.tsx` montam objetos `EmpresaComigo` completos. Acrescente aos objetos `BASE` de cada um:

```ts
  proximoPasso: null,
  proximoPassoData: null,
  vencido: false,
```

- [ ] **Step 5: Rode e veja passar**

```
npm run typecheck
npm run test:integracao -- fila-consulta
npm run test:unit
```

Esperado: PASS.

- [ ] **Step 6: Commit**

```bash
git branch --show-current
git add src/features/fila/consulta.ts tests/integracao/fila-consulta.test.ts app/fila/cartao.test.tsx app/carteira/lista.test.tsx
git commit -m "feat(fila): a carteira traz o próximo passo derivado e a ordem da agenda"
```

---

### Task 9: A action única, e a catraca que a vigia

**Files:**
- Create: `src/features/contato/acao.ts`
- Modify: `src/server/diretivas.test.ts:23`
- Create: `src/features/contato/um-chamador.test.ts`

**Interfaces:**
- Produces: `type EstadoContato = { erro: string | null; ok: boolean }`, `registrarContatoAcao(anterior: EstadoContato, form: FormData): Promise<EstadoContato>`.

- [ ] **Step 1: Escreva a catraca do chamador único**

Crie `src/features/contato/um-chamador.test.ts`:

```ts
import { globSync, readFileSync } from 'node:fs'
import { describe, expect, test } from 'vitest'

// Duas telas registram contato — /fila e /carteira/[id] — e as duas devem
// passar pela MESMA action e pelo MESMO repositório. Esta catraca pega o caso
// concreto: uma segunda tela chamando o banco por conta própria.
//
// LIMITE DECLARADO: não pega duas actions IMPORTANDO o mesmo repositório e
// validando cada uma do seu jeito. Aí `contato_registrar` continua num arquivo
// só e a regra está em dois. Não há catraca boa para isso — exigiria contar
// chamadores de uma função de domínio, e o tsc não expõe isso sem ferramenta
// nova. A defesa é `regras.ts` ser função pura testada. Isso é BILHETE.
const ARQUIVOS = [...globSync('src/**/*.ts'), ...globSync('app/**/*.{ts,tsx}')].filter(
  (caminho) => !caminho.includes('.test.'),
)

const COM_LITERAL = ARQUIVOS.filter((c) => readFileSync(c, 'utf8').includes('contato_registrar'))

describe('contato_registrar tem um chamador só', () => {
  // Sem este, renomear a função deixa a catraca verde vigiando zero arquivos.
  // Mesma lição da lista de políticas irrestritas que a `cep` deixou vazia.
  test('existe pelo menos um arquivo com o literal, senão esta catraca não vigia nada', () => {
    expect(COM_LITERAL.length).toBeGreaterThan(0)
  })

  test('o literal aparece em exatamente um arquivo fora de tests/', () => {
    expect(COM_LITERAL).toEqual(['src/features/contato/repositorio.ts'])
  })
})
```

- [ ] **Step 2: Rode e veja passar (ou falhar por caminho)**

Run: `npm run test:unit -- um-chamador`
Esperado: PASS. Se falhar por separador de caminho no Windows, normalize com `.map((c) => c.replaceAll('\\', '/'))` antes do filtro.

- [ ] **Step 3: Escreva o teste de diretiva da action**

A action em si não ganha teste unitário: ela chama `exigir`, que lê cookie, e o projeto não tem harness para isso — o que cobre a action é a verificação manual, como já está registrado em `divida-tecnica.md` para `agirNaFilaAcao`. O que **tem** teste é a forma do arquivo, e ele é o passo 6.

Acrescente a `src/features/contato/um-chamador.test.ts`:

```ts
// A regra de validação mora em regras.ts, função pura, e a action só a chama.
// Se alguém copiar a validação para dentro da action, este teste não pega —
// é o LIMITE declarado acima. O que ele pega é a action deixar de usar a
// regra, que é a metade automatizável.
test('a action importa a regra pura em vez de validar por conta propria', () => {
  const fonte = readFileSync('src/features/contato/acao.ts', 'utf8')
  expect(fonte).toContain("from './regras'")
})
```

- [ ] **Step 4: Rode e veja falhar**

Run: `npm run test:unit -- um-chamador`
Esperado: FAIL, `ENOENT: src/features/contato/acao.ts`.

- [ ] **Step 5: Implemente a action**

Crie `src/features/contato/acao.ts`:

```ts
'use server'

import { revalidatePath } from 'next/cache'
import { exigir } from '../../server/autenticacao/guarda'
import { textoDaFalta, textoDoMotivo } from './mensagens'
import { validar } from './regras'
import { registrarContato } from './repositorio'

export type EstadoContato = { erro: string | null; ok: boolean }

// A action ÚNICA das duas telas. Mora em src/features e não em app/ porque a
// convenção de action junto da rota serve quando há UMA rota; com duas, pô-la
// em app/carteira/[id]/ criaria uma dependência de rota para rota que ninguém
// adivinha lendo /fila.
export async function registrarContatoAcao(
  _anterior: EstadoContato,
  form: FormData,
): Promise<EstadoContato> {
  const eu = await exigir('usuario')
  const empresaId = String(form.get('id') ?? '')
  const r = validar({
    tipo: String(form.get('tipo') ?? ''),
    desfecho: String(form.get('desfecho') ?? ''),
    nota: String(form.get('nota') ?? ''),
    proximoPasso: String(form.get('proximoPasso') ?? ''),
    proximoPassoData: String(form.get('proximoPassoData') ?? ''),
    posse: form.get('posse') === 'sim',
  })
  if (!r.ok) return { erro: textoDaFalta(r.falta), ok: false }

  const gravado = await registrarContato(eu.usuarioId, empresaId, r.valor)
  if (!gravado.ok) return { erro: textoDoMotivo(gravado.motivo), ok: false }

  revalidatePath('/fila')
  revalidatePath('/carteira')
  revalidatePath(`/carteira/${empresaId}`)
  return { erro: null, ok: true }
}
```

**Atenção — arquivo `'use server'` só exporta função, e `EstadoContato` é `type`, que some na compilação.** Nenhuma constante é exportada daqui: os textos moram em `mensagens.ts` justamente por isso. Exportar uma constante faria o Next transformá-la em referência de servidor — o bug que `src/server/diretivas.test.ts` existe para pegar, e que a partir do próximo passo passa a ser pego também neste arquivo.

O estado inicial (`INICIAL`) **não** mora aqui: ele é definido dentro do componente cliente, em `formulario.tsx`, que é o que `app/usuarios/formulario-criar.tsx:5` sempre fez e o que o comentário de lá registra.

- [ ] **Step 6: Estenda o glob da catraca de diretivas**

Modifique `src/server/diretivas.test.ts:23`:

```ts
// Varre `app/**` e `src/**`: a action de contato mora em src/features/contato,
// e sem esta linha ela ficaria fora do alcance da catraca que nasceu
// exatamente do bug que ela pode ter. Antes desta fatia a catraca afirmava
// "toda action em app/ está vigiada"; agora afirma "toda superfície
// 'use server' do projeto está vigiada".
const ARQUIVOS = [...globSync('app/**/*.{ts,tsx}'), ...globSync('src/**/*.{ts,tsx}')].filter((caminho) => {
  const primeira = readFileSync(caminho, 'utf8').trimStart().slice(0, 20)
  return primeira.startsWith("'use server'") || primeira.startsWith('"use server"')
})
```

Acrescente ao mesmo arquivo um teste que prove que a extensão cobre alguma coisa:

```ts
  // Sem isto, estender o glob passa e não cobre nada: a catraca ficaria verde
  // sobre os mesmos arquivos de antes.
  test('a varredura enxerga a action que mora em src/', () => {
    expect(ARQUIVOS.some((c) => c.replaceAll('\\', '/') === 'src/features/contato/acao.ts')).toBe(true)
  })
```

- [ ] **Step 7: Rode e veja passar**

```
npm run typecheck
npm run test:unit -- diretivas
npm run test:unit -- src/features/contato
```

Esperado: PASS.

- [ ] **Step 8: Commit**

```bash
git branch --show-current
git add src/features/contato/acao.ts src/features/contato/um-chamador.test.ts src/server/diretivas.test.ts
git commit -m "feat(contato): a action única, e a catraca de diretivas passa a varrer src/"
```

---

### Task 10: O formulário compartilhado e a linha do tempo

**Files:**
- Create: `src/features/contato/formulario.tsx`
- Create: `src/features/contato/formulario.test.tsx`
- Create: `src/features/contato/linha-do-tempo.tsx`
- Create: `src/features/contato/linha-do-tempo.test.tsx`
- Modify: `vitest.config.mts` (o projeto `unitario` precisa incluir `src/**/*.test.tsx`)

**Interfaces:**
- Produces: `<FormularioContato empresaId={string} posse={boolean} />`, `<LinhaDoTempo contatos={Contato[]} />`.

- [ ] **Step 1: Inclua `src/**/*.test.tsx` no projeto unitário**

Modifique `vitest.config.mts`, no projeto `unitario`:

```ts
          include: ['src/**/*.test.ts', 'src/**/*.test.tsx', 'app/**/*.test.tsx'],
```

- [ ] **Step 2: Escreva os testes de render**

Crie `src/features/contato/linha-do-tempo.test.tsx` (molde: `app/fila/cartao.test.tsx`):

```tsx
import { renderToStaticMarkup } from 'react-dom/server'
import { describe, expect, test } from 'vitest'
import type { Contato } from './historico'
import { LinhaDoTempo } from './linha-do-tempo'

const UM: Contato = {
  id: '11111111-1111-4111-8111-111111111111',
  tipo: 'nao_atendeu',
  nota: 'caiu na caixa postal',
  proximoPasso: null,
  proximoPassoData: null,
  criadoEm: new Date('2026-09-08T12:00:00Z'),
  autor: 'VendedorA',
}

describe('LinhaDoTempo', () => {
  test('lista vazia diz que ninguem ligou ainda, e nao fica em branco', () => {
    const saida = renderToStaticMarkup(<LinhaDoTempo contatos={[]} />)
    expect(saida).toContain('Nenhum contato registrado')
  })

  test('mostra o rotulo do tipo, a nota e o autor', () => {
    const saida = renderToStaticMarkup(<LinhaDoTempo contatos={[UM]} />)
    expect(saida).toContain('Liguei, não falei com ninguém')
    expect(saida).toContain('caiu na caixa postal')
    expect(saida).toContain('VendedorA')
  })

  test('mostra o proximo passo combinado quando ha', () => {
    const saida = renderToStaticMarkup(
      <LinhaDoTempo contatos={[{ ...UM, proximoPasso: 'Mandar orçamento', proximoPassoData: '2026-10-01' }]} />,
    )
    expect(saida).toContain('Mandar orçamento')
    expect(saida).toContain('2026-10-01')
  })

  // Tipo fora da lista pode existir no banco: o CHECK só exige não-vazio.
  // A tela não pode quebrar por causa disso.
  test('tipo desconhecido cai no proprio texto, sem quebrar', () => {
    const saida = renderToStaticMarkup(<LinhaDoTempo contatos={[{ ...UM, tipo: 'inventado' }]} />)
    expect(saida).toContain('inventado')
  })
})
```

Crie `src/features/contato/formulario.test.tsx`:

```tsx
import { renderToStaticMarkup } from 'react-dom/server'
import { describe, expect, test, vi } from 'vitest'

// useActionState trocado por mock: o que se prova é qual ramo renderiza para
// cada estado, não que o estado chegue. Mesmo limite dos outros testes de
// render deste projeto.
vi.mock('react', async () => {
  const real = await vi.importActual<typeof import('react')>('react')
  return { ...real, useActionState: () => [{ erro: null, ok: false }, () => {}, false] }
})

const { FormularioContato } = await import('./formulario')

describe('FormularioContato', () => {
  test('sem posse, NAO pede proximo passo', () => {
    const saida = renderToStaticMarkup(<FormularioContato empresaId="e1" posse={false} />)
    expect(saida).not.toContain('Próximo passo')
  })

  test('com posse, PEDE proximo passo e data', () => {
    const saida = renderToStaticMarkup(<FormularioContato empresaId="e1" posse />)
    expect(saida).toContain('Próximo passo')
    expect(saida).toContain('name="proximoPassoData"')
  })

  test('sem posse oferece os cinco tipos de reserva, e nao o de posse', () => {
    const saida = renderToStaticMarkup(<FormularioContato empresaId="e1" posse={false} />)
    expect(saida).toContain('value="nao_liguei"')
    expect(saida).toContain('value="interessado"')
    expect(saida).not.toContain('value="acompanhamento"')
  })

  test('com posse oferece acompanhamento', () => {
    const saida = renderToStaticMarkup(<FormularioContato empresaId="e1" posse />)
    expect(saida).toContain('value="acompanhamento"')
  })

  // A promessa que o sistema não cumpre. Sem esta frase o vendedor acredita
  // que agendou alguma coisa.
  test('avisa que a data de retornar_depois nao agenda nada', () => {
    const saida = renderToStaticMarkup(<FormularioContato empresaId="e1" posse={false} />)
    expect(saida).toContain('volta para a fila em 30 dias')
  })

  test('leva o id da empresa e a posse como campos ocultos', () => {
    const saida = renderToStaticMarkup(<FormularioContato empresaId="e1" posse />)
    expect(saida).toContain('value="e1"')
    expect(saida).toContain('name="posse"')
  })
})
```

- [ ] **Step 3: Rode e veja falhar**

Run: `npm run test:unit -- src/features/contato`
Esperado: FAIL, imports não resolvidos.

- [ ] **Step 4: Implemente `linha-do-tempo.tsx`**

```tsx
import type { Contato } from './historico'
import { ROTULO, ehTipo } from './tipos'

function rotulo(tipo: string): string {
  // O banco só exige não-vazio, então tipo fora da lista é possível. A tela
  // mostra o texto cru em vez de quebrar.
  return ehTipo(tipo) ? ROTULO[tipo] : tipo
}

export function LinhaDoTempo({ contatos }: { contatos: Contato[] }) {
  if (contatos.length === 0) {
    return (
      <p className="rounded border border-dashed p-3 text-sm text-neutral-600">
        Nenhum contato registrado nesta empresa ainda.
      </p>
    )
  }
  return (
    <ol className="flex flex-col gap-2">
      {contatos.map((c) => (
        <li key={c.id} className="rounded border p-3 text-sm">
          <p className="font-medium">{rotulo(c.tipo)}</p>
          {c.nota ? <p className="text-neutral-700">{c.nota}</p> : null}
          {c.proximoPasso ? (
            <p className="text-neutral-700">
              Próximo passo: {c.proximoPasso} — {c.proximoPassoData}
            </p>
          ) : null}
          <p className="text-neutral-500">
            {c.autor ?? 'sistema'} · {c.criadoEm.toISOString().slice(0, 10)}
          </p>
        </li>
      ))}
    </ol>
  )
}
```

- [ ] **Step 5: Implemente `formulario.tsx`**

```tsx
'use client'

import { useActionState, useState } from 'react'
import { registrarContatoAcao, type EstadoContato } from './acao'
import { DESFECHO_SUGERIDO, EXIGE_POSSE, ROTULO, TIPOS_CONTATO, type TipoContato } from './tipos'

const INICIAL: EstadoContato = { erro: null, ok: false }

type Props = { empresaId: string; posse: boolean }

export function FormularioContato({ empresaId, posse }: Props) {
  const oferecidos = TIPOS_CONTATO.filter((t) => EXIGE_POSSE[t] === posse)
  const [tipo, setTipo] = useState<TipoContato>(oferecidos[0])
  const [estado, agir, pendente] = useActionState(registrarContatoAcao, INICIAL)

  return (
    <form action={agir} className="flex flex-col gap-3 border-t pt-3">
      <input type="hidden" name="id" value={empresaId} />
      <input type="hidden" name="posse" value={posse ? 'sim' : 'nao'} />
      <input type="hidden" name="desfecho" value={DESFECHO_SUGERIDO[tipo]} />

      {estado.erro ? (
        <p className="rounded border border-amber-300 bg-amber-50 p-3 text-sm">{estado.erro}</p>
      ) : null}

      <fieldset className="flex flex-col gap-1">
        <legend className="text-sm font-medium">O que aconteceu na ligação</legend>
        {oferecidos.map((t) => (
          <label key={t} className="flex items-center gap-2 text-sm">
            <input
              type="radio"
              name="tipo"
              value={t}
              checked={tipo === t}
              onChange={() => setTipo(t)}
            />
            {ROTULO[t]}
          </label>
        ))}
      </fieldset>

      <label className="flex flex-col gap-1 text-sm">
        Nota
        <textarea name="nota" rows={2} className="rounded border p-2" />
      </label>

      {posse ? (
        <>
          <label className="flex flex-col gap-1 text-sm">
            Próximo passo
            <input type="text" name="proximoPasso" className="rounded border p-2" />
          </label>
          <label className="flex flex-col gap-1 text-sm">
            Data do próximo passo
            <input type="date" name="proximoPassoData" className="rounded border p-2" />
          </label>
        </>
      ) : (
        <>
          <label className="flex flex-col gap-1 text-sm">
            Combinado (opcional)
            <input type="text" name="proximoPasso" className="rounded border p-2" />
          </label>
          <label className="flex flex-col gap-1 text-sm">
            Data combinada (opcional)
            <input type="date" name="proximoPassoData" className="rounded border p-2" />
          </label>
          {/* A promessa que o sistema NÃO cumpre, dita com todas as letras.
              Sem esta frase o vendedor acredita que agendou alguma coisa. */}
          <p className="text-sm text-neutral-600">
            A empresa volta para a fila em 30 dias. A data fica registrada no histórico e não agenda nada.
          </p>
        </>
      )}

      <button type="submit" disabled={pendente} className="self-start rounded border px-3 py-2 text-sm font-medium">
        {pendente ? 'Registrando…' : 'Registrar'}
      </button>
    </form>
  )
}
```

- [ ] **Step 6: Rode e veja passar**

```
npm run typecheck
npm run test:unit -- src/features/contato
```

Esperado: PASS.

- [ ] **Step 7: Commit**

```bash
git branch --show-current
git add src/features/contato/formulario.tsx src/features/contato/formulario.test.tsx src/features/contato/linha-do-tempo.tsx src/features/contato/linha-do-tempo.test.tsx vitest.config.mts
git commit -m "feat(contato): formulário compartilhado e linha do tempo, com render testado"
```

---

### Task 11: `/fila` — o cartão ganha histórico e formulário

**Files:**
- Modify: `app/fila/cartao.tsx`
- Modify: `app/fila/formulario.tsx`
- Modify: `app/fila/page.tsx`
- Modify: `app/fila/cartao.test.tsx`, `app/fila/formulario.test.tsx`

**Interfaces:**
- Consumes: `FormularioContato`, `LinhaDoTempo`, `lerHistorico`, `Contato`.
- Produces: `<Cartao empresa agora contatos />` — o cartão passa a receber o histórico e a renderizar o formulário.

**R-018:** antes de escrever, abra `app/usuarios/formulario-criar.tsx` e `app/fila/formulario.tsx` inteiros, comentários inclusive. O estado inicial de `useActionState` é definido **dentro do componente cliente**, nunca exportado de arquivo `'use server'` — o comentário em `app/usuarios/formulario-criar.tsx:5` registra o bug que isso já custou.

- [ ] **Step 1: Escreva o teste**

Acrescente a `app/fila/cartao.test.tsx`:

```tsx
test('o cartao mostra o historico de quem ligou antes', () => {
  const saida = renderToStaticMarkup(
    <Cartao
      empresa={BASE}
      agora={AGORA}
      contatos={[
        {
          id: '22222222-2222-4222-8222-222222222222',
          tipo: 'sem_interesse',
          nota: 'ja tem fornecedor',
          proximoPasso: null,
          proximoPassoData: null,
          criadoEm: new Date('2026-08-01T12:00:00Z'),
          autor: 'VendedorA',
        },
      ]}
    />,
  )
  expect(saida).toContain('ja tem fornecedor')
  expect(saida).toContain('VendedorA')
})

test('cartao sem historico diz que ninguem ligou ainda', () => {
  const saida = renderToStaticMarkup(<Cartao empresa={BASE} agora={AGORA} contatos={[]} />)
  expect(saida).toContain('Nenhum contato registrado')
})

test('o cartao NAO tem mais botoes soltos de assumir e devolver', () => {
  const saida = renderToStaticMarkup(<Cartao empresa={BASE} agora={AGORA} contatos={[]} />)
  expect(saida).not.toContain('value="assumir"')
  expect(saida).not.toContain('value="devolver"')
})
```

- [ ] **Step 2: Rode e veja falhar**

Run: `npm run test:unit -- cartao`
Esperado: FAIL — `contatos` não é prop de `Cartao`.

- [ ] **Step 3: Implemente**

Em `app/fila/cartao.tsx`: acrescente `contatos: Contato[]` a `Props`, importe `LinhaDoTempo` e `FormularioContato`, **remova a `<div>` com os dois botões** e ponha no lugar:

```tsx
      <section className="flex flex-col gap-2">
        <h3 className="text-sm font-medium">O que já aconteceu com esta empresa</h3>
        <LinhaDoTempo contatos={contatos} />
      </section>
      <FormularioContato empresaId={empresa.id} posse={empresa.posse} />
```

O `Cartao` deixa de estar dentro do `<form>` de `app/fila/formulario.tsx` — `FormularioContato` traz o próprio `<form>`, e formulário aninhado é HTML inválido. Em `app/fila/formulario.tsx`, tire o `<input type="hidden" name="id">` e o `<Cartao>` de dentro do `<form>`, deixando o `<form>` só com o botão "Puxar próxima" no ramo sem reserva:

```tsx
  if (reserva) {
    return <Cartao empresa={reserva} agora={new Date()} contatos={contatos} />
  }
  return (
    <form action={agir} className="flex flex-col gap-3">
      {/* ...o ramo vazio como já era, com o botão puxar... */}
    </form>
  )
```

`Formulario` passa a receber `contatos` por prop. Em `app/fila/page.tsx`, busque o histórico quando houver reserva:

```tsx
  const r = await lerMinhasEmpresas(eu.usuarioId)
  const historico = r.ok && r.reserva ? await lerHistorico(eu.usuarioId, r.reserva.id) : null
  const contatos = historico?.ok ? historico.contatos : []
```

- [ ] **Step 4: Rode e veja passar**

```
npm run typecheck
npm run lint
npm run test:unit
```

Esperado: PASS.

- [ ] **Step 5: Commit**

```bash
git branch --show-current
git add app/fila/
git commit -m "feat(fila): o cartão mostra o histórico e registra o contato na mesma tela"
```

---

### Task 12: `/carteira` — a agenda

**Files:**
- Modify: `app/carteira/lista.tsx`
- Modify: `app/carteira/page.tsx`
- Modify: `app/carteira/lista.test.tsx`

- [ ] **Step 1: Escreva o teste**

Acrescente a `app/carteira/lista.test.tsx` (reuse o `BASE` que já existe no arquivo, já com os três campos novos da Task 8):

```tsx
const COM_PASSO = { ...BASE, id: 'a', razaoSocial: 'Alfa', proximoPasso: 'Mandar orçamento', proximoPassoData: '2026-10-01', vencido: false, posse: true }
const VENCIDA = { ...BASE, id: 'b', razaoSocial: 'Beta', proximoPasso: 'Ligar de volta', proximoPassoData: '2026-09-01', vencido: true, posse: true }
const SEM_PASSO = { ...BASE, id: 'c', razaoSocial: 'Gama', proximoPasso: null, proximoPassoData: null, vencido: false, posse: true }

test('mostra o proximo passo e a data de cada empresa', () => {
  const saida = renderToStaticMarkup(<ListaCarteira linhas={[COM_PASSO]} />)
  expect(saida).toContain('Mandar orçamento')
  expect(saida).toContain('2026-10-01')
})

test('marca a vencida com palavra, nao so com cor', () => {
  const saida = renderToStaticMarkup(<ListaCarteira linhas={[VENCIDA]} />)
  expect(saida).toContain('vencido')
})

// NULLS LAST é decisão: empresa sem próximo passo precisa de atenção, mas
// menos que um combinado vencido. Para não sumir no fim, o grupo é CONTADO e
// NOMEADO.
test('o grupo sem proximo passo aparece contado e nomeado', () => {
  const saida = renderToStaticMarkup(<ListaCarteira linhas={[COM_PASSO, SEM_PASSO]} />)
  expect(saida).toContain('1 sem próximo passo')
})

test('nao mostra o aviso de grupo quando todas tem proximo passo', () => {
  const saida = renderToStaticMarkup(<ListaCarteira linhas={[COM_PASSO]} />)
  expect(saida).not.toContain('sem próximo passo')
})

test('cada empresa linka para a ficha', () => {
  const saida = renderToStaticMarkup(<ListaCarteira linhas={[COM_PASSO]} />)
  expect(saida).toContain('/carteira/a')
})
```

- [ ] **Step 2: Rode e veja falhar**

Run: `npm run test:unit -- carteira`
Esperado: FAIL.

- [ ] **Step 3: Implemente**

Em `app/carteira/lista.tsx`, mantenha o estado vazio como está e troque o corpo da `<ul>`:

```tsx
  const semPasso = linhas.filter((l) => l.proximoPasso === null).length
  return (
    <>
      {semPasso > 0 ? (
        <p className="rounded border border-dashed p-3 text-sm text-neutral-600">
          {semPasso} sem próximo passo — assumidas e ainda sem nada combinado.
        </p>
      ) : null}
      <ul className="flex flex-col gap-2">
        {linhas.map((l) => (
          <li key={l.id} className="rounded border p-3 text-sm">
            <Link href={`/carteira/${l.id}`} className="font-medium underline">
              {l.razaoSocial}
            </Link>
            <p className="text-neutral-600">
              {l.telefone}
              {l.endereco ? ` — ${l.endereco.localidade}/${l.endereco.uf}` : ''}
            </p>
            {l.proximoPasso ? (
              <p className={l.vencido ? 'text-amber-700' : 'text-neutral-700'}>
                {l.proximoPasso} — {l.proximoPassoData}
                {l.vencido ? ' (vencido)' : ''}
              </p>
            ) : (
              <p className="text-neutral-500">Sem próximo passo combinado.</p>
            )}
          </li>
        ))}
      </ul>
    </>
  )
```

`app/carteira/page.tsx` não muda: a ordem já vem do banco.

- [ ] **Step 4: Rode e veja passar**

```
npm run typecheck
npm run lint
npm run test:unit
```

- [ ] **Step 5: Commit**

```bash
git branch --show-current
git add app/carteira/
git commit -m "feat(carteira): a carteira vira a agenda, com o grupo sem próximo passo contado"
```

---

### Task 13: `/carteira/[id]` — a ficha

**Files:**
- Create: `app/carteira/[id]/page.tsx`
- Create: `app/carteira/[id]/ficha.tsx`
- Create: `app/carteira/[id]/ficha.test.tsx`

**Interfaces:**
- Consumes: `lerMinhasEmpresas`, `lerHistorico`, `LinhaDoTempo`, `FormularioContato`.
- Produces: a rota `/carteira/[id]`.

**Fronteira:** a página compõe `src/features/fila` (o que está comigo) e `src/features/contato` (o que aconteceu). Nenhuma das duas features importa a outra.

- [ ] **Step 1: Escreva o teste de render da ficha**

Crie `app/carteira/[id]/ficha.test.tsx`:

```tsx
import { renderToStaticMarkup } from 'react-dom/server'
import { describe, expect, test } from 'vitest'
import type { EmpresaComigo } from '@/src/features/fila/consulta'
import { Ficha } from './ficha'

const EMPRESA: EmpresaComigo = {
  id: 'a',
  cnpj: '11222333000181',
  razaoSocial: 'Aurora Comercio LTDA',
  nomeFantasia: 'Aurora Luz',
  contatoNome: 'Dona Aurora',
  telefone: '11987654321',
  email: null,
  cep: '01310100',
  endereco: null,
  reservadoAte: null,
  posse: true,
  proximoPasso: 'Mandar orçamento',
  proximoPassoData: '2026-10-01',
  vencido: false,
}

describe('Ficha', () => {
  test('mostra cadastro, telefone e CNPJ', () => {
    const saida = renderToStaticMarkup(<Ficha empresa={EMPRESA} contatos={[]} />)
    expect(saida).toContain('Aurora Comercio LTDA')
    expect(saida).toContain('11987654321')
    expect(saida).toContain('11222333000181')
  })

  test('mostra o proximo passo que vale hoje', () => {
    const saida = renderToStaticMarkup(<Ficha empresa={EMPRESA} contatos={[]} />)
    expect(saida).toContain('Mandar orçamento')
  })

  // O gap da fila.1 que esta fatia fecha: hoje não existe caminho de tela para
  // devolver empresa da carteira.
  test('tem o caminho de devolver, que a carteira nao tinha', () => {
    const saida = renderToStaticMarkup(<Ficha empresa={EMPRESA} contatos={[]} />)
    expect(saida).toContain('value="devolver"')
  })

  test('com posse, o formulario pede proximo passo', () => {
    const saida = renderToStaticMarkup(<Ficha empresa={EMPRESA} contatos={[]} />)
    expect(saida).toContain('Próximo passo')
  })
})
```

- [ ] **Step 2: Rode e veja falhar**

Run: `npm run test:unit -- ficha`
Esperado: FAIL, import não resolvido.

- [ ] **Step 3: Implemente a ficha**

Crie `app/carteira/[id]/ficha.tsx`. O botão de devolver é um `FormularioContato` com o tipo de posse e desfecho `devolver` — ou, mais simples e é o que este plano manda: um segundo `FormularioContato` não nasce; a ficha oferece o formulário normal, e o campo oculto `desfecho` é substituído por dois botões de envio com `name="desfecho"`:

```tsx
import type { Contato } from '@/src/features/contato/historico'
import { LinhaDoTempo } from '@/src/features/contato/linha-do-tempo'
import { FormularioContato } from '@/src/features/contato/formulario'
import type { EmpresaComigo } from '@/src/features/fila/consulta'

export function Ficha({ empresa, contatos }: { empresa: EmpresaComigo; contatos: Contato[] }) {
  return (
    <article className="flex flex-col gap-4">
      <header className="flex flex-col gap-1">
        <h2 className="text-lg font-semibold">{empresa.razaoSocial}</h2>
        {empresa.nomeFantasia ? <p className="text-sm text-neutral-600">{empresa.nomeFantasia}</p> : null}
      </header>
      <dl className="grid grid-cols-[auto_1fr] gap-x-3 gap-y-1 text-sm">
        <dt className="text-neutral-600">Telefone</dt>
        <dd className="font-medium">{empresa.telefone}</dd>
        {empresa.contatoNome ? (
          <>
            <dt className="text-neutral-600">Contato</dt>
            <dd>{empresa.contatoNome}</dd>
          </>
        ) : null}
        <dt className="text-neutral-600">CNPJ</dt>
        <dd>{empresa.cnpj}</dd>
      </dl>
      {empresa.proximoPasso ? (
        <p className={empresa.vencido ? 'text-sm text-amber-700' : 'text-sm'}>
          Próximo passo: {empresa.proximoPasso} — {empresa.proximoPassoData}
          {empresa.vencido ? ' (vencido)' : ''}
        </p>
      ) : (
        <p className="text-sm text-neutral-600">Sem próximo passo combinado.</p>
      )}
      <section className="flex flex-col gap-2">
        <h3 className="text-sm font-medium">Linha do tempo</h3>
        <LinhaDoTempo contatos={contatos} />
      </section>
      <FormularioContato empresaId={empresa.id} posse={empresa.posse} comDevolver />
    </article>
  )
}
```

Acrescente a prop `comDevolver?: boolean` a `src/features/contato/formulario.tsx`. Quando ligada, troque o `<input type="hidden" name="desfecho">` por dois botões de envio:

```tsx
      {comDevolver ? (
        <div className="flex gap-2">
          <button type="submit" name="desfecho" value={DESFECHO_SUGERIDO[tipo]} disabled={pendente} className="rounded border px-3 py-2 text-sm font-medium">
            Registrar
          </button>
          <button type="submit" name="desfecho" value="devolver" disabled={pendente} className="rounded border px-3 py-2 text-sm">
            Registrar e devolver
          </button>
        </div>
      ) : (
        <>
          <input type="hidden" name="desfecho" value={DESFECHO_SUGERIDO[tipo]} />
          <button type="submit" disabled={pendente} className="self-start rounded border px-3 py-2 text-sm font-medium">
            {pendente ? 'Registrando…' : 'Registrar'}
          </button>
        </>
      )}
```

Acrescente ao teste da Task 10 um caso para `comDevolver`, no mesmo arquivo `src/features/contato/formulario.test.tsx`:

```tsx
test('com comDevolver, oferece o segundo botao de desfecho', () => {
  const saida = renderToStaticMarkup(<FormularioContato empresaId="e1" posse comDevolver />)
  expect(saida).toContain('value="devolver"')
})
```

- [ ] **Step 4: Implemente a página**

Crie `app/carteira/[id]/page.tsx`. Molde de navegação: `app/carteira/page.tsx`.

```tsx
import Link from 'next/link'
import { notFound } from 'next/navigation'
import { lerHistorico } from '@/src/features/contato/historico'
import { lerMinhasEmpresas } from '@/src/features/fila/consulta'
import { textoDoMotivo } from '@/src/features/fila/mensagens'
import { exigir } from '@/src/server/autenticacao/guarda'
import { Ficha } from './ficha'

export default async function PaginaFicha({ params }: { params: Promise<{ id: string }> }) {
  const { id } = await params
  const eu = await exigir('usuario')
  const r = await lerMinhasEmpresas(eu.usuarioId)
  if (!r.ok) {
    return (
      <main className="mx-auto flex w-full max-w-2xl flex-1 flex-col gap-6 p-6">
        <p className="rounded border border-amber-300 bg-amber-50 p-3 text-sm">{textoDoMotivo(r.motivo)}</p>
      </main>
    )
  }
  const empresa = r.carteira.find((e) => e.id === id)
  // Empresa fora da carteira é 404 e não "sem permissão": a existência dela
  // não é informação que o vendedor deva receber.
  if (!empresa) notFound()
  const historico = await lerHistorico(eu.usuarioId, id)

  return (
    <main className="mx-auto flex w-full max-w-2xl flex-1 flex-col gap-6 p-6">
      <header className="flex items-center justify-between">
        <h1 className="text-2xl font-semibold">Ficha</h1>
        <nav className="flex items-center gap-3">
          <Link href="/carteira" className="text-sm underline">
            Carteira
          </Link>
          <Link href="/fila" className="text-sm underline">
            Fila
          </Link>
        </nav>
      </header>
      <Ficha empresa={empresa} contatos={historico.ok ? historico.contatos : []} />
    </main>
  )
}
```

R-004: `params` é tipado na mão como `Promise<{ id: string }>`, nunca com tipo gerado pelo build.

- [ ] **Step 5: Rode e veja passar**

```
npm run typecheck
npm run lint
npm run build
npm run test:unit
```

Esperado: PASS nos quatro. O `build` entra aqui porque é a primeira rota dinâmica do projeto.

- [ ] **Step 6: Commit**

```bash
git branch --show-current
git add app/carteira/ src/features/contato/formulario.tsx src/features/contato/formulario.test.tsx
git commit -m "feat(carteira): a ficha da empresa, com linha do tempo e o devolver que faltava"
```

---

### Task 14: Documentação

**Files:**
- Create: `docs/db/0017.md`, `docs/db/0018.md`, `docs/db/0019.md`
- Modify: `docs/db/0016.md` (ponteiro de encaminhamento, R-015)
- Modify: `docs/db/fundacao.md`
- Modify: `docs/db/divida-tecnica.md`

- [ ] **Step 1: Escreva os três docs de migração**

Cada um no formato dos existentes (abra `docs/db/0016.md` como molde). Conteúdo mínimo de cada:

- **`0017.md`** — por que uma tabela e não duas (o `crm-ch` separava por fase do funil, e não há funil aqui); por que `proximo_passo_data` e não `_em` (a exceção do `_em` que a `fila.1` abriu não se copia sem argumento próprio); por que `date` e não `timestamptz` (meio-dia em São Paulo é gambiarra para guardar dia em tipo de instante); por que a política é uma frase que delega; por que não há máscara de `nota` (o motivo da máscara do `crm-ch` era `GRANT` por coluna, que este projeto não tem).
- **`0018.md`** — a ordem das checagens como contrato; o desfecho antes do `INSERT` e o motivo (`RETURN` não desfaz `INSERT` em plpgsql); a trava em `empresa` e o caminho de deadlock que teste de uma conexão não vê; `IS DISTINCT FROM` em vez de `=` na checagem de posse.
- **`0019.md`** — as duas funções ficaram sem chamador e a R-014 manda o `GRANT` sair; revogar remove uma opção real (devolver sem registrar), e isso é deliberado, com a tabela de abandonar × devolver; **lista os objetos tocados fora do próprio escopo** (as duas funções da 0016), como a R-015 exige do doc novo.

- [ ] **Step 2: Ponteiro na `0016.md`**

Uma linha logo abaixo do título de `docs/db/0016.md` (R-015), com **as duas** mudanças:

```markdown
> **Alterada depois.** A `0019` revogou o `GRANT` de `empresa_assumir` e
> `empresa_devolver` para `app_usuario`: as duas continuam existindo e passaram
> a ser chamadas só por dentro de `contato_registrar` (`0018`). E o gatilho 4
> desta fatia — teto de carteira em 200 — foi substituído pelo gatilho C da
> fatia `contato`. Ver `docs/db/0019.md`.
```

- [ ] **Step 3: `fundacao.md`**

Seção "Feito na fatia `contato`", no formato das outras: a tabela, a função, a política que delega, e a revogação.

- [ ] **Step 4: `divida-tecnica.md`**

Quatro blocos, no formato das fatias anteriores:

1. **"Fatia contato: o que acontece com os gatilhos da fila.1"** — os cinco, um a um, com o gatilho 1 aposentado e substituído (numerador novo, remédio novo escrito) e o gatilho 4 trocado. Remova o `200` da tabela de gatilhos vivos.
2. **"Fatia contato: os gatilhos novos"** — a tabela A–E da spec, com `7` e `80` escritos e rotulados como chute, e `80` com a nota de estar estruturalmente mudo enquanto a base for 65.
3. **"Fatia contato: fragilidades herdadas"** — a lista da spec, começando pelo **sintoma** da que mais vai virar reclamação: *"o vendedor devolve uma empresa, lembra que anotou algo importante nela, e não consegue mais ver"*. E a frouxidão do próximo passo anulável **como uma só**, com os dois lugares dentro dela e o conserto único.
4. **"Fatia contato: o que os testes NÃO provam"** — nenhum teste clica; o formulário é testado com `useActionState` trocado por mock; a corrida `devolver` × `fila_puxar` continua sem teste; nada roda contra Postgres 18.6.

- [ ] **Step 5: Rode a catraca de prosa**

Run: `npm run test:unit -- docs`
Esperado: PASS. Se acusar caractere de controle, é a R-020 — ilustre o ponto de código pelo nome (`U+0000`), nunca pelo caractere.

- [ ] **Step 6: Commit**

```bash
git branch --show-current
git add docs/
git commit -m "docs: 0017, 0018 e 0019, o ponteiro na 0016 e os gatilhos da fatia contato"
```

---

### Task 15: Verificação manual e PR

- [ ] **Step 1: Suba o ambiente**

```
npm run db:subir
npm run db:aplicar
npm run db:cep:carregar
npm run db:seed:gestor
npm run dev
```

Importe as 65 empresas por `/empresas/importar` como gestor, e crie dois vendedores por `/usuarios`.

**Não rode a suíte de testes durante a verificação manual** — o harness reescreve a senha do `app_conexao` e derruba a `DATABASE_URL` do dev.

- [ ] **Step 2: Percorra os doze passos**

Os doze da spec, na ordem. Registre o que passou e o que não passou.

1. Vendedor A puxa; registra `nao_atendeu` com desfecho devolver.
2. A empresa some da fila de A e ele deixa de ler os contatos dela.
3. A puxa outra; registra `interessado` com desfecho assumir; ela aparece em `/carteira` **sem** próximo passo, no grupo contado.
4. Abre a ficha; registra `acompanhamento` com próximo passo para ontem; a carteira mostra como vencido, no topo.
5. Registra outro `acompanhamento` com data futura; o vencido some e a data nova aparece.
6. Registra `acompanhamento` sem próximo passo: **a tela recusa**, porque há posse.
7. Devolve pela ficha; sai da carteira.
8. Vendedor B puxa até receber a empresa do passo 1; **lê o contato de A na tela**.
9. `retornar_depois` com data: a tela avisa que a empresa volta em 30 dias e a data é só histórico.
10. Tentar registrar sem escolher tipo: a tela recusa antes de chamar o banco.
11. Usuário com senha provisória pendente tenta registrar; recusa com mensagem.
12. Gestor continua vendo as 65 em `/empresas`, e não tem ficha.

- [ ] **Step 3: Registre o resultado**

Acrescente a `docs/db/divida-tecnica.md` a seção **"Fatia contato: verificado à mão"**, no formato das anteriores (tabela com passo, o que se esperava, e se passou).

- [ ] **Step 4: Rode tudo uma vez**

```
npm run db:checar
npm run typecheck
npm run lint
npm run build
npm test
```

- [ ] **Step 5: Commit e PR**

```bash
git branch --show-current
git add docs/db/divida-tecnica.md
git commit -m "docs: verificação manual da fatia contato"
git push -u origin fatia-contato-spec
gh pr create --base main --title "Fatia contato: o que aconteceu na ligação e o próximo passo" --body "..."
```

O PR é contra a `main` desde o começo, sem empilhar (R-021). O corpo cita a spec e resume: uma tabela, uma função, a revogação de duas, três telas, e o gatilho 1 da `fila.1` fechado.

---

## Auto-revisão deste plano

**Cobertura da spec.** Cada seção tem tarefa: a tabela e a política (1), o vocabulário (2), a regra do próximo passo (3), a função e a ordem do contrato (4), a revogação (5), repositório e mensagens (6), a linha do tempo (7), o próximo passo derivado e a ordem da agenda (8), a action única e as duas catracas (9), formulário e render (10), `/fila` (11), `/carteira` (12), `/carteira/[id]` (13), documentação e gatilhos (14), verificação manual (15).

**Três desvios da spec, todos deliberados e nenhum de comportamento:**

1. **Três migrações em vez de uma**, pela R-011 — migração é imutável depois de aplicada, e três tarefas editariam o mesmo arquivo.
2. **`vitest.config.mts` ganha `src/**/*.test.tsx`** (Task 10). A spec não previu: os componentes compartilhados moram em `src/features/contato/`, e o projeto `unitario` só incluía `.test.tsx` de `app/`.
3. **`app/fila/acoes.ts` é podado na Task 5**, antes da Task 11 refazer a tela. Sem isso a Task 5 fecharia com o `typecheck` vermelho.

**Duas coisas que só aparecem executando, e vão avisadas:**

- **A Task 5 quebra `fila-posse.test.ts` de propósito.** A reescrita está na própria tarefa; quem executar fora de ordem vai achar que quebrou algo.
- **`globSync` no Windows devolve `\` como separador.** As duas catracas novas comparam caminho com string literal; se falhar, normalize com `replaceAll('\\', '/')`. Está dito nos passos.
